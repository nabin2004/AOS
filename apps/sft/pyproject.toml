[project]
name = "sft"
version = "0.1.0"
description = "Gemma 4 LoRA fine-tuning on Manim instruction chat pairs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "accelerate>=1.0.0",
    "bitsandbytes>=0.45.0",
    "datasets>=5.0.0",
    "google-genai>=0.1.0",
    "huggingface-hub>=0.27.0",
    "openai>=1.50.0",
    "peft>=0.19.1",
    "python-dotenv>=1.0.0",
    "torch>=2.5.0",
    "transformers>=4.51.0",
    "trl>=1.9.0",
    "wandb>=0.19.0",
    "wrapt>=1.17.0",
]

[project.optional-dependencies]
liger = ["liger-kernel>=0.5.0"]

[[tool.uv.index]]
name = "pytorch-cu124"
url = "https://download.pytorch.org/whl/cu124"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cu124" }
torchvision = { index = "pytorch-cu124" }
torchaudio = { index = "pytorch-cu124" }

[build-system]
requires = ["setuptools>=69.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = [
    "chat_template",
    "codemode_contract",
    "config",
    "data",
    "filter_codemode",
    "infer",
    "infer_tools",
    "export_gguf",
    "hub_upload",
    "merge_adapter",
    "model",
    "narrate_trajectories_batch",
    "preflight_gemma4",
    "run",
    "run_narrated_pipeline",
    "trainer",
    "upload_dataset",
    "upload_narrated_dataset",
]
