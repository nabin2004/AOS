{"id": "sample_31", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene358(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the padding strategy: padding left side to maintain sequence length for causal convolutions.\n        title = Title(\"Show the padding strategy: padding left \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_sigmoid'/> to plot the sigmoid activation function, defined as sigma of x equals one over one plus e to the negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_23", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene247(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the splitting of a dataset into three colored blocks (Train=Blue, Dev=Green, Test=Red) with a rotating separator.\n        title = Title(\"Animate the splitting of a dataset into \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. We start by setting up our coordinate axes, plotting the sigmoid curve, and displaying its mathematical formula: sigma of x equals one divided by the quantity one plus e to the power of negative x. <bookmark mark='MOVE_DOT'/> Now, watch as we move a point along the curve to see how its value approaches one as x increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"MOVE_DOT\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_19", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene29(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the hierarchical feature extraction: low-level (characters) \u2192 mid-level (morphology) \u2192 high-level (semantics).\n        title = Title(\"Show the hierarchical feature extraction\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> and plotting the sigmoid activation function, which is defined as sigma of x equals one over one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve as the input value increases towards two.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_22", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene293(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the effective rank of activations, showing how many dimensions are actually used by the layer.\n        title = Title(\"Visualize the effective rank of activati\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes to visualize the activation function. \"\n                 \"<bookmark mark='show_curve'/> Here, we plot the sigmoid function, which maps any real-valued number to a value between zero and one. \"\n                 \"<bookmark mark='show_label'/> The mathematical formula is sigma of x equals one divided by the quantity, one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center, where the input is zero and the output is zero point five.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the activation value changes as we increase the input. \"\n                 \"<bookmark mark='move_dot'/> As the input moves to two, the sigmoid activation approaches one.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_37", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene201(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the Tanh activation function curve mapping input logits (-inf, inf) to outputs (-1, 1), showing saturation zones in red.\n        title = Title(\"Animate the Tanh activation function cur\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function, which maps input values to a range between zero and one. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the curve defined by sigma of x equals one divided by one plus e to the power of negative x. \"\n                 \"<bookmark mark='move_dot'/> Watch how the red dot moves along the curve as the input increases, showing how the output approaches one.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_38", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene342(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show `log_softmax` applied to a vector, highlighting the numerical stability tricks (subtracting max).\n        title = Title(\"Show `log_softmax` applied to a vector, \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function, which is commonly used in neural networks. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve alongside its mathematical formula, \"\n                 \"one over, one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Watch how the red dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_79", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene343(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the \"dilation\" as a spring stretching the filter taps apart.\n        title = Title(\"Visualize the \\\"dilation\\\" as a spring str\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, \"\n                 \"<bookmark mark='show_label'/> defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the sigmoid curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_58", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene382(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Create a side-by-side comparison of human-written text vs. bigram-generated text, color-coding differences.\n        title = Title(\"Create a side-by-side comparison of huma\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin <bookmark mark='show_all'/> by setting up our coordinate axes, plotting the sigmoid curve, and displaying its mathematical formula, while placing a red dot at the center.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_all\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve as the input value increases towards two.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_101", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene339(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the updated weights after a manual step, comparing them to the PyTorch autograd result side-by-side.\n        title = Title(\"Visualize the updated weights after a ma\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> and plotting the sigmoid activation function, defined as sigma of x equals one divided by, one plus e to the power of negative x. <bookmark mark='move_dot'/> Now, let us observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_65", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene291(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the derivation of the Xavier formula, showing the variance propagation derivation step-by-step.\n        title = Title(\"Animate the derivation of the Xavier for\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"<bookmark mark='show_elements'/> We begin by setting up our coordinate system to visualize the sigmoid activation function, which is defined as sigma of x equals one divided by one plus e to the negative x. Let's place a point at the center of the curve. <bookmark mark='move_dot'/> Now, we can observe how the point moves along the sigmoid curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_127", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene236(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the bias correction step in Adam for early training, scaling up the low initial estimates.\n        title = Title(\"Show the bias correction step in Adam fo\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by displaying the title and setting up our coordinate axes, along with the sigmoid function curve and its mathematical formula. <bookmark mark='move_dot'/> Next, we observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_134", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene329(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        title = Title(\"Show a real-time dashboard: Loss (top-le\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid function, \"\n                 \"defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Let us observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_116", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene340(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the module's `__call__` method wrapping the `forward` call with hooks and pre/post processing.\n        title = Title(\"Visualize the module's `__call__` method\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"In this visualization, we explore the module's call method, which wraps the forward pass. \"\n                 \"<bookmark mark='SHOW_AXES'/> We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='SHOW_LABEL'/> represented as sigma of x equals one divided by, one plus e to the power of negative x. \"\n                 \"<bookmark mark='SHOW_DOT'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"SHOW_AXES\")\n            self.play(Create(ax), Create(curve))\n            self.wait_until_bookmark(\"SHOW_LABEL\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"SHOW_DOT\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the sigmoid curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_129", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene249(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        title = Title(\"Show the effect of `requires_grad=True` \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"To understand how PyTorch tracks gradients when requires grad is set to true, let us visualize a function like the sigmoid activation. <bookmark mark='show_graph'/> Here we have the sigmoid curve, and a point representing our tensor's value.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_graph\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"As we move the tensor's value along the curve, PyTorch tracks these operations to compute gradients during the backward pass.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_148", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene69(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the iterative prediction loop for an autoregressive model, where the output token at step T becomes the input token at step T+1.\n        title = Title(\"Animate the iterative prediction loop fo\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the starting position. \"\n                 \"<bookmark mark='move_dot'/> Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_158", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene22(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the Hessian eigenvalues as an ellipse, with gradient steps adapting to curvature.\n        title = Title(\"Visualize the Hessian eigenvalues as an \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the function by setting up our coordinate axes, plotting the sigmoid curve, and placing a starting point at the center. <bookmark mark='move_dot'/> Then, we can observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_164", "narrated_manim_code": "from manim import *\nimport numpy as np\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\n\nclass AOSTrajectoryScene24(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the process of adding `assert` statements to check tensor shapes and values during forward/backward.\n        title = Title(\"Animate the process of adding `assert` s\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the sigmoid curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_256", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene225(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a summary table of hyperparameter configurations ranked by validation accuracy, sorted ascending.\n        title = Title(\"Show a summary table of hyperparameter c\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us begin by displaying the title and setting up our coordinate axes, \"\n                 \"<bookmark mark='show_curve'/> along with the sigmoid function curve and its mathematical formula, \"\n                 \"sigma of x equals one divided by, one plus e to the power of negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, <bookmark mark='move_dot'/> let us watch the dot move along the sigmoid curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_275", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene6(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the relationship between cross-entropy and KL-divergence using overlapping probability distributions.\n        title = Title(\"Illustrate the relationship between cros\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid function, which is defined as one divided by one plus e to the negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_293", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene89(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the derivative of the tanh function as a bell-shaped curve, highlighting regions of saturation (vanishing gradients).\n        title = Title(\"Visualize the derivative of the tanh fun\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the curve. \"\n                 \"<bookmark mark='show_formula'/> Here is the mathematical formula for the sigmoid function, \"\n                 \"represented by sigma of x. <bookmark mark='move_dot'/> Now, let us track a point \"\n                 \"moving along this curve towards the region of saturation.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_formula\")\n            self.play(Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_316", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene374(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the effect of fixing gamma=1 and beta=0 (pure normalization) vs. learning them.\n        title = Title(\"Illustrate the effect of fixing gamma=1 \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by illustrating the effect of fixing gamma to one and beta to zero, which represents pure normalization. \"\n                 \"<bookmark mark='show_axes'/> Here, we plot the standard sigmoid activation function, \"\n                 \"<bookmark mark='show_dot'/> with a red dot representing a normalized value. \"\n                 \"<bookmark mark='move_dot'/> Watch as the dot moves along the curve to its new position.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_axes\")\n            self.play(Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_318", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene283(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the gradient flow from the loss through the Linear layer, BN, and Tanh sequentially.\n        title = Title(\"Show the gradient flow from the loss thr\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> to plot the sigmoid activation function, defined as sigma of x equals one divided by one plus e to the negative x. <bookmark mark='move_dot'/> Now, let's observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            \n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_333", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene215(VoiceoverScene):\n    def construct(self):\n        # Setup voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show a time-unrolled RNN/Transformer cell, duplicating the network architecture across the sequence length dimension.\n        title = Title(\"Show a time-unrolled RNN/Transformer cel\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid activation function. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as sigma of x \"\n                 \"equals one divided by, one plus e to the negative x, and place a dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, watch as the dot moves along the curve, \"\n                 \"demonstrating how the output approaches one as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_364", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene250(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the effect of LR decay over epochs, with the step size shrinking like a staircase.\n        title = Title(\"Illustrate the effect of LR decay over e\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize a function on our coordinate axes. <bookmark mark='show_curve'/> Here, we plot the sigmoid activation function, defined as sigma of x equals one over, one plus e to the negative x, and place a starting point at the center. <bookmark mark='move_dot'/> Watch as the point transitions along the curve to its new position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            \n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_386", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene381(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the effect of BN on gradient magnitude: preventing the scale from exploding in early layers (shown as equalized arrows).\n        title = Title(\"Visualize the effect of BN on gradient m\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the effect of batch normalization on gradient magnitude. We start by plotting the sigmoid activation function, defined as sigma of x equals one divided by one plus e to the power of minus x. <bookmark mark='move_dot'/> Now, watch how a point moves along this curve as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_382", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene143(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the \"gate\" as a sliding door that opens (close to 1) or closes (close to 0) based on input.\n        title = Title(\"Visualize the \\\"gate\\\" as a sliding door t\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We can visualize the gate as a sliding door that opens or closes based on the input. \"\n                 \"Let's set up our coordinate system <bookmark mark='show_curve'/> and plot the sigmoid function, \"\n                 \"defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a starting dot on the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"As the input increases, <bookmark mark='move_dot'/> the dot slides up the curve towards one, representing an open gate.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_388", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene400(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the three-step derivative for BN: output \u2192 normalized \u2192 variance/mean \u2192 input.\n        title = Title(\"Animate the three-step derivative for BN\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function, defined as sigma of x equals one divided by one plus e to the negative x. <bookmark mark='move_dot'/> Now, we observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_393", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene135(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the backward pass through BN by hand, showing the complex derivative terms decomposed into simple operations.\n        title = Title(\"Animate the backward pass through BN by \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function, defined as sigma of x equals one divided by the sum of one and e to the power of negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_394", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene187(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the forward pass data flow through a 3-layer MLP, with input vector expanding and contracting through hidden layer sizes.\n        title = Title(\"Show the forward pass data flow through \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, defined as sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_418", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene68(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the effect of a poor initialization on Tanh: all outputs saturating to \u00b11, represented as a flat line.\n        title = Title(\"Show the effect of a poor initialization\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"To understand the effect of a poor initialization, let us look at the sigmoid activation function. \"\n                 \"<bookmark mark='show_graph'/> Here, we plot the sigmoid curve, defined as sigma of x equals \"\n                 \"one divided by the quantity one plus e to the negative x, starting with an initial point. \"\n                 \"<bookmark mark='move_dot'/> As the input increases, the output moves along the curve, \"\n                 \"approaching the saturation region.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_graph\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_399", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene63(VoiceoverScene):\n    def construct(self):\n        # Setup voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the generated text quality score (e.g., BLEU-1) as a moving gauge needle.\n        title = Title(\"Visualize the generated text quality sco\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, \"\n                 \"which maps any real-valued number to a value between zero and one. \"\n                 \"<bookmark mark='SHOW_DOT'/> We also place a red dot at the center of the curve, \"\n                 \"where the input is zero and the output is point five.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"SHOW_DOT\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve as the input value increases to two, \"\n                 \"causing the output to approach one.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_424", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene325(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the causal mask: a triangular matrix of zeros above the diagonal, preventing future token leakage.\n        title = Title(\"Visualize the causal mask: a triangular \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by displaying our title and setting up the coordinate axes. \"\n                 \"<bookmark mark='show_curve'/> Next, we plot the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x, \"\n                 \"and place a red dot at the y-intercept. \"\n                 \"<bookmark mark='move_dot'/> Finally, we animate the dot as it moves along the curve to a new position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_434", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene189(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the embedding initialization (uniform distribution) and how it evolves after 1000 training steps.\n        title = Title(\"Animate the embedding initialization (un\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid function. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as sigma of x \"\n                 \"equals one divided by the quantity one plus e to the negative x, and place a red dot at the origin. \"\n                 \"<bookmark mark='move_dot'/> Next, we watch how the dot moves along the curve as the input value increases to two.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_426", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene317(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the generated text string being typed out character by character, with a glowing cursor indicating the next predicted character's probability distribution.\n        title = Title(\"Show the generated text string being typ\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid activation function on our coordinate axes, starting with a point at the center. \"\n                 \"<bookmark mark='show_label'/> The mathematical expression is defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Watch how the point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_448", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene364(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the gradient flow through a residual connection: the gradient bypasses the weight layers, preserving magnitude.\n        title = Title(\"Animate the gradient flow through a resi\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We start by setting up our coordinate axes, <bookmark mark='show_curve'/> then we plot the sigmoid curve along with its mathematical formula. <bookmark mark='move_dot'/> Finally, we can observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_449", "narrated_manim_code": "from manim import *\nimport numpy as np\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\n\nclass AOSTrajectoryScene128(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Create a heatmap of activation correlations across neurons in a layer, showing co-adaptation.\n        title = Title(\"Create a heatmap of activation correlati\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        # Voiceover narration with bookmarks\n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid activation function, <bookmark mark='show_graph'/> \"\n                 \"defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"Let's place a point at the center, <bookmark mark='move_dot'/> \"\n                 \"and watch it transition as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_graph\")\n            self.play(\n                Write(title), \n                Create(ax), \n                Create(curve), \n                Write(label), \n                FadeIn(dot),\n                run_time=2\n            )\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=2\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_436", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene285(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate a `conv2d` operation on a small image, showing the kernel sliding and the dot product lighting up the output pixel.\n        title = Title(\"Animate a `conv2d` operation on a small \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the sigmoid activation function. We plot the axes, \"\n                 \"<bookmark mark='curve'/> the sigmoid curve, and its mathematical formula, \"\n                 \"<bookmark mark='dot'/> along with a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, watch as the dot moves along the curve towards the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"curve\")\n            self.play(Create(curve))\n            self.wait_until_bookmark(\"dot\")\n            self.play(Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_472", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene369(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a histogram of sequence lengths generated over 100 independent sampling runs.\n        title = Title(\"Show a histogram of sequence lengths gen\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid function, \"\n                 \"<bookmark mark='show_label'/> defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center where the input is zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve as the input value increases to two.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_473", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene375(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the input shape transformation: (N, H) \u2192 (N, H) with normalization applied independently per feature.\n        title = Title(\"Animate the input shape transformation: \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> to plot the sigmoid activation function, defined as sigma of x equals one divided by one plus e to the negative x. <bookmark mark='show_dot'/> We place a red dot at the center, <bookmark mark='move_dot'/> and then trace its path along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_485", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene229(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the relationship between cross-entropy and KL-divergence, adding a constant entropy term.\n        title = Title(\"Illustrate the relationship between cros\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_elements'/> and plotting the sigmoid activation function, which maps real-valued inputs to probabilities. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_481", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene191(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the fan_in vs fan_out tradeoff in different initialization variants (Xavier vs. He).\n        title = Title(\"Illustrate the fan_in vs fan_out tradeof\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"To understand the fan in versus fan out tradeoff in neural network initialization, let us first visualize the sigmoid activation function. <bookmark mark='show_axes'/> Here, we set up our coordinate axes, <bookmark mark='show_curve'/> plot the sigmoid curve, <bookmark mark='show_label'/> and place a starting point at the center.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_axes\")\n            self.play(Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how an input value propagates through the activation function, moving the point along the curve towards saturation.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_487", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene221(VoiceoverScene):\n    def construct(self):\n        # Setup speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize a debugger breakpoint pausing the forward pass, displaying current layer outputs.\n        title = Title(\"Visualize a debugger breakpoint pausing \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We can visualize a debugger breakpoint pausing the forward pass of a neural network. \"\n                 \"<bookmark mark='show_graph'/> Here, we plot the sigmoid activation function, defined as \"\n                 \"sigma of x equals one over one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Let's trace the activation value as it moves along the curve.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_graph\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=max(1.5, tracker.time_remaining())\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_500", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene119(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the manual gradient update for a single embedding row, shifting it in the embedding space.\n        title = Title(\"Illustrate the manual gradient update fo\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid function, \"\n                 \"<bookmark mark='show_curve'/> defined as sigma of x equals one divided by one plus e to the power of negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place an initial point on the curve at x equals zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, we illustrate a manual gradient update, shifting our point along the sigmoid curve to a new position.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_514", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene188(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize a residual block as a main highway plus a skip connection branching around the weight layers.\n        title = Title(\"Visualize a residual block as a main hig\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system. <bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as sigma of x equals one divided by the quantity one plus e to the negative x, and place a red dot at the center. <bookmark mark='move_dot'/> Now, watch as the dot moves along the curve towards the right, representing the activation value as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_551", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene9(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a \"loss landscape\" 3D terrain for a small language model, with the optimizer's trajectory descending into a valley.\n        title = Title(\"Show a \\\"loss landscape\\\" 3D terrain for a\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. We start by setting up our coordinate axes, plotting the sigmoid curve, and placing a red dot at the center. <bookmark mark='move_dot'/> Then, we watch the dot transition along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_557", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene334(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        title = Title(\"Animate the initialization of a multilay\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> to visualize the sigmoid activation function, defined as sigma of x equals one over one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center of the curve, representing the initial state.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the output changes <bookmark mark='move_dot'/> as we increase the input value along the curve.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_576", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene197(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the skip connections from each block summing to the final output layer.\n        title = Title(\"Animate the skip connections from each b\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, defined as sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='dot_move'/> Next, we can observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_566", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene272(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a table of input values and their corresponding gradients for a simple linear regression scenario, updating row by row.\n        title = Title(\"Show a table of input values and their c\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_label'/> Here is the mathematical definition of the sigmoid function, sigma of x. \"\n                 \"<bookmark mark='show_dot'/> We place a point at the center of the curve, \"\n                 \"<bookmark mark='move_dot'/> and then watch it transition as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            \n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            \n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_571", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene286(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the gradient noise scale (batch size effect) as a jittering arrow tip.\n        title = Title(\"Animate the gradient noise scale (batch \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='MOVE_DOT'/> Next, we observe how a point moves along this activation curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"MOVE_DOT\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=tracker.get_remaining_duration()\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_610", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene79(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate a confusion matrix morphing into a precision/recall table for character prediction.\n        title = Title(\"Animate a confusion matrix morphing into\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='dot_move'/> Now, we can observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_613", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene337(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the chain rule as a backward flow of gradients through a two-layer expression tree, highlighting each derivative multiplied along the path.\n        title = Title(\"Visualize the chain rule as a backward f\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_elements'/> to visualize the sigmoid activation function, sigma of x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as we transition from the origin to a higher input value.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_622", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene172(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the calculation of batch mean and variance for a single feature dimension across the batch axis.\n        title = Title(\"Animate the calculation of batch mean an\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes, plotting the sigmoid activation function, and placing a red dot at the center. <bookmark mark='move_dot'/> Next, we watch the dot transition along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_645", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene151(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the gradient of NLL w.r.t. logits: `dL/dz = softmax(z) - y`, animating the error vector pointing from prediction to target.\n        title = Title(\"Show the gradient of NLL w.r.t. logits: \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the gradient of the negative log likelihood with respect to the logits, which simplifies to the softmax of z minus y. <bookmark mark='show_elements'/> We begin by drawing the coordinate axes, plotting the sigmoid activation function, and placing a red dot at the origin. <bookmark mark='move_dot'/> Now, we animate the dot moving along the curve to represent the change in prediction as the input logit increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_612", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene255(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the calculation of negative log probability for a specific training pair, breaking down the exponentiation and log operations.\n        title = Title(\"Animate the calculation of negative log \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the sigmoid function. We first set up our coordinate axes <bookmark mark='show_curve'/> and plot the sigmoid curve, <bookmark mark='show_label'/> defined as sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center where x is zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let's observe how the value changes as we move the input x to two. <bookmark mark='move_dot'/> The dot slides along the curve to its new position.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_621", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene268(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the dilation factor as expanding gaps between filter taps, growing exponentially with depth.\n        title = Title(\"Show the dilation factor as expanding ga\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_label'/> This function is defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve as the input x increases to two.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_647", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene121(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the computation of the normalized output: `(x - mean) / sqrt(var + eps)` broken down into three substeps.\n        title = Title(\"Illustrate the computation of the normal\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. We start by setting up our coordinate axes, plotting the sigmoid curve, and showing its mathematical formula, sigma of x equals one divided by one plus e to the negative x. We also place a red dot at the center. <bookmark mark='move_dot'/> Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_700", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene97(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the standardization step: subtract mean (shift to 0) and divide by std (scale to 1) for a batch of features.\n        title = Title(\"Animate the standardization step: subtra\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, defined as sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='dot_move'/> Let's observe how a point on this curve moves as we increase the input value.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_666", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene48(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the gradients of gamma and beta as tracking the optimal scale and shift for the layer.\n        title = Title(\"Show the gradients of gamma and beta as \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the activation function. \"\n                 \"<bookmark mark='plot_elements'/> Here, we plot the sigmoid curve along with its mathematical definition. \"\n                 \"<bookmark mark='show_dot'/> A red dot is placed at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"plot_elements\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"<bookmark mark='move_dot'/> Now, watch as the dot moves along the trajectory, representing how the parameters adjust during optimization.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))),\n                run_time=tracker.duration\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_711", "narrated_manim_code": "from manim import *\nimport numpy as np\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\n\nclass AOSTrajectoryScene30(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the `.grad` attribute of a tensor accumulating gradients from multiple loss terms (e.g., regularization + data loss).\n        title = Title(\"Visualize the `.grad` attribute of a ten\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"To visualize how values change, let us set up our coordinate system, plot the sigmoid function, and place an initial point at the center. <bookmark mark='move_dot'/> We can then track this point as it moves along the curve to a new position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_667", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene40(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the broadcasting rule: a shape (1, 3) tensor expanding to match a (4, 3) matrix, duplicating rows.\n        title = Title(\"Animate the broadcasting rule: a shape (\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. We start by drawing the coordinate axes, \"\n                 \"<bookmark mark='PLOT_CURVE'/> plotting the sigmoid curve, and displaying its mathematical formula, \"\n                 \"sigma of x equals one divided by, one plus e to the power of minus x. \"\n                 \"<bookmark mark='SHOW_DOT'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"PLOT_CURVE\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"SHOW_DOT\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"<bookmark mark='MOVE_DOT'/> Now, watch as the dot moves along the curve to its new position.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"MOVE_DOT\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_714", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene159(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the stack of dilated convolutions, showing how a single output at the top can see the entire sequence.\n        title = Title(\"Animate the stack of dilated convolution\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Watch how the dot moves along the curve as we increase the input value.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_719", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene196(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create a block diagram of the loss surface for a mini-batch, aggregating individual sample losses into a single scalar.\n        title = Title(\"Create a block diagram of the loss surfa\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function. <bookmark mark='show_label'/> This function is defined as sigma of x equals one divided by the sum of one and e to the negative x. <bookmark mark='move_dot'/> Now, let's observe how the output value changes as we move the point along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_727", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene315(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Display a line chart of training vs. validation loss over epochs, clearly marking the point of overfitting.\n        title = Title(\"Display a line chart of training vs. val\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the sigmoid function. We set up our coordinate axes, plot the characteristic S-shaped curve, and write its mathematical formula, placing a red dot at the center. <bookmark mark='move_dot'/> Now, as we increase the input value, the dot slides along the curve, asymptotically approaching one.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_758", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene391(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        title = Title(\"Show a code-like pseudocode block of the\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"In this demonstration, we will visualize the sigmoid activation function. \"\n                 \"<bookmark mark='show_elements'/> First, we set up our coordinate axes, plot the sigmoid curve, \"\n                 \"and display its mathematical formula, sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"We also place a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Next, we animate the dot as it moves along the curve to a new position.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(\n                Write(title), \n                Create(ax), \n                Create(curve), \n                Write(label), \n                FadeIn(dot)\n            )\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=2\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_742", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene21(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate `softmax` as a temperature-controlled process: high temp (flat distribution) to low temp (spiky one-hot).\n        title = Title(\"Animate `softmax` as a temperature-contr\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by displaying the title. <bookmark mark='axes'/> Next, we construct the coordinate axes and plot the sigmoid curve. <bookmark mark='formula'/> Here is the mathematical definition: sigma of x equals one divided by the quantity, one plus e to the power of negative x. <bookmark mark='dot'/> We place a red dot at the center. <bookmark mark='move'/> Finally, we animate the dot moving along the curve to show how the function behaves as the input increases.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"axes\")\n            self.play(Create(ax), Create(curve))\n            self.wait_until_bookmark(\"formula\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_785", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene176(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a comparison of outputs for the same input in train vs. eval mode, revealing slight differences.\n        title = Title(\"Show a comparison of outputs for the sam\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. We first draw the axes and the curve, \"\n                 \"<bookmark mark='show_label'/> then we display the formula for sigma of x, \"\n                 \"<bookmark mark='show_dot'/> and finally place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Next, we animate the dot as it moves along the sigmoid curve to evaluate a new input value.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_821", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene105(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the memory efficiency of in-place gradient accumulation vs. allocating new tensors.\n        title = Title(\"Illustrate the memory efficiency of in-p\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us begin by setting up our coordinate system and plotting the sigmoid function. \"\n                 \"<bookmark mark='show_formula'/> Here is the mathematical formula for sigma of x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_formula\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"<bookmark mark='move_dot'/> Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_833", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene379(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a color-coded grid of activations for a mini-batch, where black indicates dead neurons.\n        title = Title(\"Show a color-coded grid of activations f\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the sigmoid activation function. We plot the axes, the characteristic S-shaped curve, and its mathematical formula, sigma of x equals one divided by one plus e to the negative x. We also place a red dot at the center. <bookmark mark='move_dot'/> Now, let's observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_798", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene206(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the output logits over the vocabulary as a horizontal bar chart for each time step during generation.\n        title = Title(\"Visualize the output logits over the voc\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the sigmoid function. We start by drawing the coordinate axes and plotting the curve. <bookmark mark='show_label'/> The function is defined as sigma of x equals one divided by the sum of one and e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center of the curve, where x is zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, watch as we move the dot along the curve to see how the sigmoid value increases as x becomes larger.\"\n        ) as tracker:\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))),\n                run_time=tracker.duration\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_849", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene136(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the formula `H(p,q) = -sum(p * log(q))` with a focusing effect on the correct label's term.\n        title = Title(\"Animate the formula `H(p,q) = -sum(p * l\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, plotting the sigmoid activation function, and placing a red dot at the center. <bookmark mark='move_dot'/> Now, watch as the dot moves along the curve towards the right, demonstrating how the output approaches one as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_894", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene328(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the parameter updates as small nudges on a 2D contour plot of the loss function.\n        title = Title(\"Animate the parameter updates as small n\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid function, \"\n                 \"defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='dot_move'/> Watch as we trace a point along this curve to visualize how the output changes.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_887", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene348(VoiceoverScene):\n    def construct(self):\n        # Setup speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the adaptive learning rate component: dividing by the root mean square of past gradients.\n        title = Title(\"Animate the adaptive learning rate compo\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by the sum of one and e to the power of negative x. \"\n                 \"<bookmark mark='move_dot'/> Let us now observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_873", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene383(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the gradient flow through residual connections: a direct tributary bypassing obstacles.\n        title = Title(\"Animate the gradient flow through residu\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the sigmoid function. We start by setting up our axes. \"\n                 \"<bookmark mark='curve'/> Next, we plot the sigmoid curve, defined as sigma of x equals \"\n                 \"one divided by one plus e to the power of minus x. \"\n                 \"<bookmark mark='dot'/> We also place a starting point on the curve at x equals zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let's observe how the value changes as we move the point along the curve towards the right.\"\n        ) as tracker:\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))),\n                run_time=tracker.duration\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_907", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene274(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate k-fold cross-validation, where the dataset rotates into k different train/dev partitions.\n        title = Title(\"Animate k-fold cross-validation, where t\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='move_dot'/> Now, observe how a point on the curve moves as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n        \n        self.wait(1)", "status": "success"}
{"id": "sample_912", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene279(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate a summary of dead neurons percentage per layer, with a warning threshold line at 30%.\n        title = Title(\"Animate a summary of dead neurons percen\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, defined as sigma of x equals one divided by the quantity of one plus e to the power of negative x. <bookmark mark='SHOW_DOT'/> We also place a red dot at the center, where x is zero and the output is zero point five.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"SHOW_DOT\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"<bookmark mark='MOVE_DOT'/> Now, let us observe how the dot moves along the curve as the input value increases towards two.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"MOVE_DOT\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_921", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene32(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the manual calculation of NLL gradient for a single sample with a one-hot target.\n        title = Title(\"Illustrate the manual calculation of NLL\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by illustrating the manual calculation of the negative log likelihood. \"\n                 \"<bookmark mark='show_axes'/> Let's set up our coordinate axes, plot the sigmoid activation function, \"\n                 \"and place an initial data point at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, watch how the data point moves along the sigmoid curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_axes\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_949", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene277(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate a trie (prefix tree) of character sequences, highlighting the depth-2 branches for the bigram model.\n        title = Title(\"Illustrate a trie (prefix tree) of chara\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. <bookmark mark='axes'/> First, we set up our coordinate axes and plot the characteristic S-shaped curve. <bookmark mark='label'/> We also display its mathematical formula, sigma of x equals one divided by the quantity one plus e to the negative x, <bookmark mark='dot'/> and place a red dot at the y-intercept.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"axes\")\n            self.play(Create(ax), Create(curve))\n            self.wait_until_bookmark(\"label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"<bookmark mark='move'/> Watch as the dot moves along the curve to the right, approaching its upper limit of one.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_952", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene211(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize a linear layer transformation geometrically: a square grid being rotated, scaled, and translated (plus bias).\n        title = Title(\"Visualize a linear layer transformation \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. <bookmark mark='show_graph'/> \"\n                 \"We set up our coordinate axes, plot the sigmoid curve, and display its mathematical formula: \"\n                 \"sigma of x equals one divided by, one plus e to the power of negative x. We also place a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, watch as the dot moves along the curve to evaluate the function at a different input value.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_graph\")\n            self.play(\n                Create(ax), \n                Create(curve), \n                Write(label), \n                FadeIn(dot)\n            )\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_967", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene398(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show exploding gradients as massive updates causing the loss to spike vertically off the chart.\n        title = Title(\"Show exploding gradients as massive upda\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, starting with a red dot at the center. <bookmark mark='move_dot'/> Then, we observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_989", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene62(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the loss as a function of a single logit value, showing the characteristic exponential tail.\n        title = Title(\"Visualize the loss as a function of a si\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes. \"\n                 \"<bookmark mark='show_curve'/> Next, we plot the sigmoid curve, defined as sigma of x equals one over one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We place a red dot at the point where x is zero and the sigmoid value is zero point five. \"\n                 \"<bookmark mark='move_dot'/> Finally, we animate the dot as it moves along the curve towards the right, showing how the function approaches its upper limit of one.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            \n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            \n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1003", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene205(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the vectorization of gradient computations: replacing nested loops with matrix operations.\n        title = Title(\"Animate the vectorization of gradient co\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one over one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Next, we can track a point along this curve as it moves to the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1013", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene299(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the final test evaluation as a single forward pass through the untouched test block, displaying final accuracy.\n        title = Title(\"Illustrate the final test evaluation as \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_label'/> The function is defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Now, let's trace the function's value by moving our red dot along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1014", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene259(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the manual chain rule derivation for a simple nested function `f(g(h(x)))`, showing intermediate variables.\n        title = Title(\"Animate the manual chain rule derivation\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"To understand the chain rule, let us first visualize a simple activation function, the sigmoid function. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as sigma of x equals one divided by the quantity one plus e to the negative x. We also place a red dot at the input value of zero. \"\n                 \"<bookmark mark='move_dot'/> Now, as we change the input, we can watch the dot trace along the curve to its new position.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(\n                Write(title),\n                Create(ax),\n                Create(curve),\n                Write(label),\n                FadeIn(dot)\n            )\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))),\n                run_time=2\n            )\n        \n        self.wait(1)", "status": "success"}
{"id": "sample_1017", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene321(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the derivative of a matrix multiplication: `dL/dW = X^T * dL/dY` and `dL/dX = dL/dY * W^T`.\n        title = Title(\"Animate the derivative of a matrix multi\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid activation function. \"\n                 \"<bookmark mark='show_elements'/> Here, we write the title, draw the axes, plot the sigmoid curve, \"\n                 \"and display its mathematical formula, sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"We also place a red dot at the center. <bookmark mark='move_dot'/> Now, let's observe how the dot \"\n                 \"moves along the sigmoid curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(\n                Write(title), \n                Create(ax), \n                Create(curve), \n                Write(label), \n                FadeIn(dot)\n            )\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=2\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1049", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene254(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate a computational graph for the expression `(a * b) + c`, showing the forward pass with numerical values propagating from input nodes to the output.\n        title = Title(\"Animate a computational graph for the ex\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, plotting the sigmoid activation function, defined as sigma of x equals one divided by one plus e to the power of negative x, and placing a point at the center. <bookmark mark='move_dot'/> Now, let's observe how this point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1048", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene308(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the training time scaling linearly with depth, shown as a bar chart.\n        title = Title(\"Visualize the training time scaling line\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, <bookmark mark='plot_sigmoid'/> and then plotting the sigmoid function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"plot_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let's watch the red dot move along the sigmoid curve towards the right.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1077", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene292(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the iterative improvement cycle: Hypothesis \u2192 Code Change \u2192 Run \u2192 Evaluate \u2192 Visualize Results.\n        title = Title(\"Illustrate the iterative improvement cyc\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid function, <bookmark mark='show_formula'/> which is defined as sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as x increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"show_formula\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1061", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene113(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the exponential receptive field growth: layer 1 covers 2, layer 2 covers 4, layer 3 covers 8.\n        title = Title(\"Illustrate the exponential receptive fie\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by displaying the title, setting up our coordinate axes, and plotting the sigmoid activation function, defined as sigma of x equals one divided by the sum of one and e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1064", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene114(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate a tree visualization of the dilated convolutions, where each node's output depends on nodes far apart.\n        title = Title(\"Animate a tree visualization of the dila\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"In this visualization, we explore the sigmoid activation function. \"\n                 \"<bookmark mark='show_graph'/> We plot the coordinate axes, the sigmoid curve, \"\n                 \"and its mathematical formula, sigma of x equals one divided by, one plus e to the power of minus x, \"\n                 \"along with a point at the origin. <bookmark mark='move_dot'/> Now, watch as the point moves \"\n                 \"along the curve, demonstrating how the output approaches one as the input increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_graph\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1105", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene310(VoiceoverScene):\n    def construct(self):\n        # Setup speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the module's training/eval mode flag propagating down to all child modules.\n        title = Title(\"Animate the module's training/eval mode \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, defined as sigma of x equals one divided by one plus e to the power of negative x. <bookmark mark='dot_move'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1082", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene294(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the dot product between an embedding vector and a weight matrix row as the \"compatibility\" score.\n        title = Title(\"Illustrate the dot product between an em\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> defined as sigma of x equals one divided by, one plus e to the power of negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, we observe the dot moving along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1111", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene160(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the backward pass through BN, showing the gradients w.r.t input, mean, variance, gamma, and beta.\n        title = Title(\"Visualize the backward pass through BN, \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"To understand the backward pass, let's first establish our coordinate system and plot the sigmoid function, \"\n                 \"<bookmark mark='show_label'/> represented by the formula sigma of x. \"\n                 \"<bookmark mark='move_dot'/> We can track a specific point on this curve as it transitions to a new value.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_1107", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene92(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the \"staircase\" of backpropagation through a deep network, with gradients shrinking as they travel backward.\n        title = Title(\"Illustrate the \\\"staircase\\\" of backpropag\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes to visualize the sigmoid activation function, \"\n                 \"<bookmark mark='show_curve'/> defined as sigma of x equals one over one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1133", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene354(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the `state_dict` and `load_state_dict` as a serialization/deserialization pipeline.\n        title = Title(\"Illustrate the `state_dict` and `load_st\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us begin by writing the title. <bookmark mark='axes'/> Next, we create the coordinate axes and plot the sigmoid curve. <bookmark mark='label'/> We also display the mathematical formula for the sigmoid function, <bookmark mark='dot'/> and place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"axes\")\n            self.play(Create(ax), Create(curve))\n            self.wait_until_bookmark(\"label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, watch as the red dot smoothly transitions <bookmark mark='move'/> along the curve to its new position.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1141", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene269(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the gradient of the gated activation, combining both Tanh and Sigmoid derivatives.\n        title = Title(\"Illustrate the gradient of the gated act\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"To illustrate the gradient of the gated activation, let us first plot the standard sigmoid function, sigma of x, which maps real numbers to values between zero and one. <bookmark mark='move_dot'/> We can observe how the function behaves by moving a point along the curve, where the slope represents its gradient.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1172", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene261(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate three different learning rates (0.001, 0.01, 0.1) on the same loss landscape, showing divergence, optimal, and slow convergence paths.\n        title = Title(\"Animate three different learning rates (\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid function, which is defined as sigma of x equals one divided by, one plus e to the power of negative x. <bookmark mark='move_dot'/> Let us now observe how a point moves along this curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_1159", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene86(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate BN allowing higher learning rates by showing a larger LR step being stable with BN (green) vs. unstable without BN (red).\n        title = Title(\"Illustrate BN allowing higher learning r\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes <bookmark mark='show_curve'/> to plot the sigmoid activation function, <bookmark mark='show_label'/> defined as sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='show_dot'/> We also place a starting point on the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the point moves along the curve as we increase the input value.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1176", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene36(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the embedding vectors of similar characters (e.g., 'a', 'e', 'i') clustering together in a 3D scatter plot.\n        title = Title(\"Show the embedding vectors of similar ch\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by setting up our coordinate axes. \"\n                 \"<bookmark mark='show_curve'/> Next, we plot the characteristic S-shaped curve, write its mathematical definition, \"\n                 \"and place a red dot at the center. <bookmark mark='move_dot'/> Now, watch as the dot moves along the curve to \"\n                 \"demonstrate how the function maps input values to outputs.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1189", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene330(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the effect of a single outlier gradient exploding, with a red spike in the bar chart.\n        title = Title(\"Illustrate the effect of a single outlie\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes, plotting the sigmoid function, and placing a red dot at the center. <bookmark mark='move_dot'/> Watch as the dot moves along the curve to illustrate its behavior.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1191", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene12(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the \"staged\" backward computation: compute local gradients first, then multiply by upstream.\n        title = Title(\"Illustrate the \\\"staged\\\" backward computa\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"To illustrate the staged backward computation, let us first set up our coordinate system. \"\n                 \"<bookmark mark='show_sigmoid'/> Here, we plot the sigmoid function, defined as sigma of x equals \"\n                 \"one divided by, one plus e to the negative x. <bookmark mark='move_dot'/> Now, let's move a point \"\n                 \"along this curve to see how we can compute its local gradient at any given position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1223", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene345(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize cross-entropy as the overlap integral between the predicted distribution (blue) and the target distribution (red, one-hot).\n        title = Title(\"Visualize cross-entropy as the overlap i\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve in blue, \"\n                 \"along with its mathematical definition, sigma of x equals one divided by \"\n                 \"the quantity, one plus e to the power of negative x, and place a red dot at the origin. \"\n                 \"<bookmark mark='move_dot'/> Next, we watch how the dot moves along the curve \"\n                 \"as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1234", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene302(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the `backward()` call as a signal propagating backward through the graph, lighting up each operation node.\n        title = Title(\"Show the `backward()` call as a signal p\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We plot the curve <bookmark mark='show_label'/> defined by sigma of x equals one divided by the quantity, one plus e to the power of negative x. <bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how this dot <bookmark mark='move_dot'/> moves along the trajectory of the sigmoid function as the input increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1239", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene384(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate `embedding` lookup as a table where a specific index highlights an entire row vector.\n        title = Title(\"Illustrate `embedding` lookup as a table\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid function. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as sigma of x \"\n                 \"equals one divided by the quantity one plus e to the negative x. We also place a red dot \"\n                 \"at the center, where x is zero. <bookmark mark='move_dot'/> Now, watch as the dot moves \"\n                 \"along the curve to the point where x equals two, showing how the function approaches its \"\n                 \"upper limit of one.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1255", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene95(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize tensor transposition as a cube rotating 90 degrees, swapping axes.\n        title = Title(\"Visualize tensor transposition as a cube\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes <bookmark mark='show_elements'/> and plotting the sigmoid activation function, defined as sigma of x equals one divided by one plus e to the negative x.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, watch as the red dot moves along the curve to its new position.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1264", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene134(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the effect of residual connections on the loss landscape: making it smoother and easier to optimize.\n        title = Title(\"Illustrate the effect of residual connec\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by displaying the title, <bookmark mark='show_elements'/> and then we plot our coordinate axes, the sigmoid activation function, and place a red dot at the center. <bookmark mark='move_dot'/> Watch as the dot smoothly transitions along the curve towards the right.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1288", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene362(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize BN's regularizing effect: adding slight noise via batch statistics, smoothing the loss landscape.\n        title = Title(\"Visualize BN's regularizing effect: addi\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, <bookmark mark='show_curve'/> plotting the sigmoid activation function, sigma of x, and placing a starting data point at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe the movement of the data point as it transitions along the curve towards the right.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1295", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene93(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the gradient flow through the normalization block: input gradients are rescaled and adjusted by batch size.\n        title = Title(\"Show the gradient flow through the norma\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid activation function on our coordinate axes, \"\n                 \"<bookmark mark='show_label'/> which is defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Now, let us observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1267", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene100(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the registration of parameters using `nn.Parameter`, highlighting them in the module's state.\n        title = Title(\"Show the registration of parameters usin\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, \"\n                 \"which is defined as sigma of x equals one over, one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1297", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene90(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the architecture of a 2-layer MLP as interconnected neuron circles, with thickness of edges representing weight magnitudes.\n        title = Title(\"Animate the architecture of a 2-layer ML\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"In this visualization, we will explore the sigmoid activation function. \"\n                 \"<bookmark mark='show_elements'/> We begin by setting up our coordinate axes, \"\n                 \"plotting the characteristic S-shaped curve, and displaying its mathematical formula: \"\n                 \"sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"We also place a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, let us observe how the output value changes \"\n                 \"as we slide the input along the curve towards the right.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1298", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene147(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the class hierarchy of a minimal neural network library: `Module` \u2192 `Neuron` \u2192 `Layer` \u2192 `MLP`, expanding each class's methods.\n        title = Title(\"Visualize the class hierarchy of a minim\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system. \"\n                 \"<bookmark mark='show_elements'/> Next, we plot the sigmoid curve, defined as sigma of x equals one divided by the quantity one plus e to the negative x, and place a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, watch how the dot smoothly transitions along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            \n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1339", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene318(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate \"teacher forcing\" by showing ground-truth tokens fed into the decoder during training (green arrows) vs. model predictions during inference (blue arrows).\n        title = Title(\"Illustrate \\\"teacher forcing\\\" by showing \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='move_dot'/> Let us observe how a point moves along this activation curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_1318", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene59(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the momentum hyperparameter controlling the running average update rate, visualized as a damping spring.\n        title = Title(\"Show the momentum hyperparameter control\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Watch how the red dot smoothly transitions along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1310", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene13(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create an animation of the top-5 predicted characters for a given context, expanding into a tree of possibilities.\n        title = Title(\"Create an animation of the top-5 predict\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid activation function on our coordinate axes. \"\n                 \"<bookmark mark='show_label'/> The function is defined as sigma of x equals \"\n                 \"one divided by, one plus e to the power of negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, watch as the dot moves along the curve <bookmark mark='move_dot'/> to its new position.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1349", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene349(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate random search vs. grid search sampling points on a 2D plane, showing coverage and efficiency.\n        title = Title(\"Animate random search vs. grid search sa\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us begin by setting up our coordinate axes, <bookmark mark='show_curve'/> plotting the characteristic S-shaped sigmoid curve, and writing its mathematical formula. <bookmark mark='move_dot'/> Now, watch how the red dot moves along the curve as we increase the input value.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1366", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene137(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the optimizer stepping: `optimizer.zero_grad()` (clearing red gradients) \u2192 `loss.backward()` (filling blue gradients) \u2192 `optimizer.step()` (weights shift).\n        title = Title(\"Visualize the optimizer stepping: `optim\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes, <bookmark mark='SHOW_CURVE'/> then we plot the sigmoid activation function, <bookmark mark='SHOW_DOT'/> and finally place our starting point on the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"SHOW_CURVE\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"SHOW_DOT\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Next, we simulate the optimizer step, moving our point along the sigmoid curve to its new position.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1395", "narrated_manim_code": "from manim import *\nimport numpy as np\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\n\nclass AOSTrajectoryScene57(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the batch matrix multiplication used in attention gradient calculations.\n        title = Title(\"Animate the batch matrix multiplication \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system. <bookmark mark='show_elements'/> Here, we plot the sigmoid activation curve, defined as one over one plus e to the negative x, and place a starting point at the center.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(\n                Create(ax), \n                Create(curve), \n                Write(label), \n                FadeIn(dot),\n                run_time=tracker.duration - tracker.get_time_until_bookmark(\"show_elements\")\n            )\n\n        with self.voiceover(\n            text=\"Now, let us observe how the activation value changes <bookmark mark='move_dot'/> as we transition the input value to the right along the curve.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=tracker.duration - tracker.get_time_until_bookmark(\"move_dot\")\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1393", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene327(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the epsilon (eps) term as a tiny buffer added to the variance to avoid division by zero.\n        title = Title(\"Visualize the epsilon (eps) term as a ti\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='move_dot'/> Let's trace a point along this curve as it approaches its upper bound.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1398", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene182(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create a parallel visualization of two bigram models (trained on different datasets) sampling the same starting character, generating diverging strings.\n        title = Title(\"Create a parallel visualization of two b\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, defined as sigma of x equals one divided by one plus e to the power of negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as x increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1406", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene240(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate a tensor slicing operation, highlighting a specific row/column being extracted from a 2D matrix.\n        title = Title(\"Animate a tensor slicing operation, high\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, defined as sigma of x equals one divided by one plus e to the negative x. <bookmark mark='move_dot'/> Let's trace the function by moving a point along the curve to see how it approaches one as x increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1416", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene388(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate a step-by-step manual gradient computation for a 2x3 weight matrix.\n        title = Title(\"Animate a step-by-step manual gradient c\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes, <bookmark mark='show_curve'/> plotting the sigmoid activation function, sigma of x equals one divided by one plus e to the negative x, <bookmark mark='show_dot'/> and placing a red dot at the starting point where x is zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the point moves along the sigmoid curve <bookmark mark='move_dot'/> as the input value x increases to two.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1437", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene373(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the weight matrix as a grid of small squares, with their colors changing (blue to red) as weights update during training.\n        title = Title(\"Visualize the weight matrix as a grid of\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> We also place a red dot at the center, which we then transition \"\n                 \"along the curve to demonstrate its activation value as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1443", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene77(VoiceoverScene):\n    def construct(self):\n        # Initialize the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Create a 3D block diagram showing how a `Neuron` encapsulates weights, bias, and the forward/backward logic.\n        title = Title(\"Create a 3D block diagram showing how a \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. <bookmark mark='show_all'/> We plot the axes, the sigmoid curve, its mathematical formula, and place a dot at the center. <bookmark mark='move_dot'/> Now, watch how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_all\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1439", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene78(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the dynamic construction of a computational graph when performing operations on tensors with `requires_grad=True`.\n        title = Title(\"Animate the dynamic construction of a co\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_sigmoid'/> to plot the sigmoid function, defined as sigma of x equals one over one plus e to the negative x. <bookmark mark='move_dot'/> Let us trace the function's value as we move along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_1411", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene144(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize beta shifting: adding beta to the output, translating the entire distribution left or right.\n        title = Title(\"Visualize beta shifting: adding beta to \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize beta shifting, which adds a bias term to the output, translating the entire distribution. We start by setting up our coordinate axes, plotting the standard sigmoid curve, and placing a red dot at the center. <bookmark mark='move_dot'/> Now, watch as we shift our position along the curve, moving the dot to its new coordinate.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1455", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene71(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show a scrolling log of epoch, loss, and accuracy, updating line by line at the bottom of the screen.\n        title = Title(\"Show a scrolling log of epoch, loss, and\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Watch how the red dot moves smoothly along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1487", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene83(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Create a performance comparison bar chart: Python loop (slow) vs. vectorized tensor operation (fast) for element-wise addition.\n        title = Title(\"Create a performance comparison bar char\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes, plotting the sigmoid activation function, and displaying its mathematical formula, sigma of x equals one divided by one plus e to the negative x. <bookmark mark='move_dot'/> Next, we can observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1451", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene33(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the character embeddings as points in a 2D PCA-projected space, clustering semantically similar characters (vowels, consonants).\n        title = Title(\"Show the character embeddings as points \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. <bookmark mark='show_elements'/> We display the title, set up the axes, plot the curve, show the formula for sigma of x, and place a red dot at the center. <bookmark mark='move_dot'/> Watch as the dot moves along the curve to a new position.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_1478", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene278(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the gradient flow through the residual stack, bypassing the convolutional layers.\n        title = Title(\"Visualize the gradient flow through the \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We visualize the gradient flow through the residual stack, bypassing the convolutional layers. Here we set up our coordinate system and plot the sigmoid function, defined as sigma of x equals one divided by one plus e to the negative x. <bookmark mark='move_dot'/> Now, watch as the red dot moves along the curve to demonstrate its activation behavior.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1491", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene3(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the entire forward and backward pass from input text to final loss, showing all intermediate tensors in a scrollable view.\n        title = Title(\"Animate the entire forward and backward \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes, <bookmark mark='show_curve'/> plotting the sigmoid activation function, and <bookmark mark='show_dot'/> placing a red dot at the center to track our position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the output changes as we move the input value along the curve <bookmark mark='move_dot'/> towards positive two.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_1517", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene338(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the training loop evaluating on the dev set after each epoch, with a vertical dashed line indicating the best checkpoint.\n        title = Title(\"Show the training loop evaluating on the\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, <bookmark mark='show_curve'/> plotting the sigmoid function, defined as sigma of x equals one divided by, one plus e to the negative x, <bookmark mark='show_dot'/> and placing a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the value changes as we move the dot along the curve to the right, where x equals two.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1522", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene38(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize gradient flow as a river system: tributaries (local gradients) merging into the main stream (upstream gradient).\n        title = Title(\"Visualize gradient flow as a river syste\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We can visualize gradient flow as a river system, where local gradients merge like tributaries. Let us plot the sigmoid function, defined as sigma of x equals one divided by the quantity one plus e to the negative x, <bookmark mark='MOVE_DOT'/> and observe how a point moves along this activation curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"MOVE_DOT\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1518", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene371(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the \"tree\" of possible generated sequences, pruning low-probability branches to show the most likely path.\n        title = Title(\"Visualize the \\\"tree\\\" of possible generat\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, plotting the sigmoid function, and placing a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n        \n        with self.voiceover(\n            text=\"Now, let us observe <bookmark mark='move_dot'/> how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1553", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene88(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the element-wise multiplication of the Tanh output and the Sigmoid gate, controlling information flow.\n        title = Title(\"Show the element-wise multiplication of \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system. <bookmark mark='show_curve'/> Then, we plot the sigmoid activation function, defined as sigma of x equals one divided by one plus e to the negative x, and place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Watch how the point moves along the curve as the input value increases, approaching a value of one.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1528", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene91(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the equation `Loss = -log(p_correct)` where the probability bar of the correct class is isolated and transformed.\n        title = Title(\"Animate the equation `Loss = -log(p_corr\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, plotting the sigmoid activation function, and placing a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the output value changes as we move the input along the curve.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1557", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene257(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the weight matrix as a heatmap and the bias vector as a side-column, combining to form the layer's output.\n        title = Title(\"Visualize the weight matrix as a heatmap\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let's visualize our function by first setting up the coordinate axes, plotting the sigmoid curve, and displaying its mathematical formula, sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Next, we can animate this dot <bookmark mark='move_dot'/> to see how the activation value changes as the input increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_1572", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene368(VoiceoverScene):\n    def construct(self):\n        # Initialize voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Create a UML class diagram for the MLP, showing inheritance from `nn.Module` and composition of layers.\n        title = Title(\"Create a UML class diagram for the MLP, \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by displaying the title, setting up our coordinate axes, plotting the sigmoid activation function, and placing a red dot at the center. <bookmark mark='move_dot'/> Then, we watch the dot transition along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1571", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene80(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Plot the training loss curve over 1000 steps, updating in real-time with an oscillating line approaching convergence.\n        title = Title(\"Plot the training loss curve over 1000 s\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, along with its mathematical formula, sigma of x equals one divided by one plus e to the negative x. <bookmark mark='move_dot'/> Let's trace the function's value by moving a point along the curve towards the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1618", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene217(VoiceoverScene):\n    def construct(self):\n        # Initialize voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the chain rule for a composite function `f(g(x))` by showing three stacked number lines with arrows connecting differentials.\n        title = Title(\"Visualize the chain rule for a composite\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid function, defined as sigma of x equals one over, one plus e to the negative x. \"\n                 \"Let's place a point at the center of the curve. <bookmark mark='move_dot'/> \"\n                 \"As we increase the input value, we can observe the point tracing along the curve towards its upper asymptote.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1597", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene5(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the gradient of the NLL loss with respect to the logits, indicating where gradients are large or small.\n        title = Title(\"Show the gradient of the NLL loss with r\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function, which is often used to map real-valued numbers into probabilities. <bookmark mark='move_dot'/> Now, watch as we increase the input value, causing the point to slide up the curve towards one.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1693", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene226(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the use of `nn.Sequential` as a container, chaining layers in a pipeline.\n        title = Title(\"Illustrate the use of `nn.Sequential` as\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We can illustrate the use of a sequential container by chaining layers in a pipeline. \"\n                 \"Here, we plot the sigmoid activation function, defined as sigma of x equals one divided by \"\n                 \"one plus e to the power of negative x. <bookmark mark='move_dot'/> Now, let us observe how \"\n                 \"an input value propagates through this function, moving the point along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_1625", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene84(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize tensor indexing using 3D coordinates, drawing a red dot moving to the indexed position in a 3D cube.\n        title = Title(\"Visualize tensor indexing using 3D coord\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. We start by setting up our coordinate axes, plotting the sigmoid curve, and showing its mathematical formula, sigma of x equals one over one plus e to the negative x. <bookmark mark='move_dot'/> Next, we watch as a red dot moves along the curve to a new position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1660", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene94(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a precision-recall curve for the most frequent characters, interpolating as the threshold varies.\n        title = Title(\"Show a precision-recall curve for the mo\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> and plotting the sigmoid activation function, defined as sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the value changes <bookmark mark='move_dot'/> as we move the dot along the curve towards the right.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1697", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene44(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize in-place operations (`add_`) modifying the original tensor memory address, vs. out-of-place operations creating a new address.\n        title = Title(\"Visualize in-place operations (`add_`) m\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> defined as sigma of x equals one over one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the sigmoid curve <bookmark mark='move_dot'/> to its new position at x equals two.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1695", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene275(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize a toggle switch between `model.train()` and `model.eval()`, highlighting that BN uses batch stats in train and running stats in eval.\n        title = Title(\"Visualize a toggle switch between `model\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. \"\n                 \"<bookmark mark='show_elements'/> Here is the curve, defined as sigma of x equals one over one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Watch as a point moves along the curve towards the upper asymptote.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1703", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene58(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the internal state of a scalar `Value` object, boxing the data, gradient, and `_prev` children nodes in a colorful UML-style diagram.\n        title = Title(\"Show the internal state of a scalar `Val\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, plotting the sigmoid activation function, and placing an initial point at the center. <bookmark mark='move_dot'/> Next, we watch the point slide along the curve to its new position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1711", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene322(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the loss value being printed to a console log in real-time during training.\n        title = Title(\"Animate the loss value being printed to \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, <bookmark mark='show_elements'/> defined as sigma of x equals one divided by, one plus e to the power of negative x. <bookmark mark='move_dot'/> Now, let us observe how a point moves along this activation curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1721", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene393(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize a custom `Linear` layer subclassing `nn.Module`, with `__init__` and `forward` methods.\n        title = Title(\"Visualize a custom `Linear` layer subcla\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid activation function, often used in custom neural network layers. <bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as sigma of x equals one divided by the quantity one plus e to the negative x, and place a red dot. <bookmark mark='move_dot'/> Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1855", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene387(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the manual forward pass through an MLP+BN, labeling each intermediate tensor with a variable name.\n        title = Title(\"Visualize the manual forward pass throug\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function, defined as sigma of x equals one divided by the sum of one and e to the negative x. <bookmark mark='move_dot'/> Let us trace the value of the function as x increases along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1762", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene281(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the receptive field growth when increasing filter size vs. increasing dilation rate.\n        title = Title(\"Animate the receptive field growth when \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let's visualize the sigmoid activation function. <bookmark mark='show_elements'/> We plot the curve of sigma of x, which is defined as one divided by the quantity one plus e to the negative x. <bookmark mark='move_dot'/> Watch how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=tracker.get_remaining_duration()\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1807", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene117(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the einsum notation for gradient computations, mapping indices to tensor contractions.\n        title = Title(\"Show the einsum notation for gradient co\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> which is defined as one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Let's observe how the function behaves as we move a point along the curve towards positive values.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_1741", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene333(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the gradient flow into the embedding table, updating only the rows corresponding to the current batch of characters.\n        title = Title(\"Visualize the gradient flow into the emb\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid curve, defined as sigma of x equals one divided by one plus e to the negative x. <bookmark mark='move_dot'/> Next, we animate a point moving along this curve to demonstrate its behavior as x increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1862", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene8(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the activation distribution for Layer 1 as a violin plot, updating in real-time over batches.\n        title = Title(\"Visualize the activation distribution fo\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the activation distribution. We begin by plotting the sigmoid function on our coordinate axes, starting with a dot at the center. <bookmark mark='show_label'/> The sigmoid function, sigma of x, is defined as one divided by the quantity, one plus e to the power of negative x. <bookmark mark='move_dot'/> Now, watch how the activation value changes as we move the dot along the curve to the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1866", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene103(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Plot the perplexity (exp(loss)) over training iterations, decreasing as the model fits the data.\n        title = Title(\"Plot the perplexity (exp(loss)) over tra\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid function, which maps any real-valued number to a value between zero and one. <bookmark mark='move_dot'/> Let's observe how the output changes as we move along the curve towards positive infinity.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1897", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene177(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the effect of weight initialization for very deep conv layers (scaling by receptive field).\n        title = Title(\"Show the effect of weight initialization\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"To understand the effect of weight initialization, let us first visualize the sigmoid activation function. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Watch how the activation value changes as we move along the curve.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1919", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene296(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a bar chart of the per-layer gradient norms, updating every epoch to reveal vanishing/exploding trends.\n        title = Title(\"Show a bar chart of the per-layer gradie\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1902", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene87(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the WaveNet stack as a series of blocks with increasing dilation rates (1, 2, 4, 8...).\n        title = Title(\"Visualize the WaveNet stack as a series \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We can visualize the activation behavior, starting with the sigmoid function, sigma of x, defined as one divided by the sum of one and e to the power of negative x. <bookmark mark='move_dot'/> Let's observe how a point on this curve behaves as we increase the input value.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1931", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene4(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the computational graph of a single training step as a massive directed acyclic graph (DAG).\n        title = Title(\"Visualize the computational graph of a s\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, <bookmark mark='plot_curve'/> plotting the sigmoid activation function, which is defined as sigma of x equals one divided by one plus e to the power of negative x, and placing a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"plot_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, we can observe how the value changes as we move the dot along the curve.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_1936", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene360(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create a step-by-step expansion of the backpropagation equations for a neuron, showing each partial derivative term explicitly.\n        title = Title(\"Create a step-by-step expansion of the b\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by setting up our coordinate axes, \"\n                 \"<bookmark mark='show_curve'/> plotting the sigmoid curve, and defining its mathematical formula, \"\n                 \"sigma of x equals one divided by the quantity, one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Now, watch as we trace a point along this activation curve to see how its value changes.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1959", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene139(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a Jupyter Notebook cell transforming into a production Python script during export.\n        title = Title(\"Show a Jupyter Notebook cell transformin\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> and plotting the sigmoid function, <bookmark mark='show_label'/> defined as sigma of x equals one divided by, one plus e to the power of negative x. <bookmark mark='show_dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Watch as the dot moves along the curve towards its upper asymptote.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1968", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene126(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the ReLU dead neuron problem: a histogram of pre-activations shifting entirely into the negative region, killing gradients.\n        title = Title(\"Animate the ReLU dead neuron problem: a \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us begin by setting up our coordinate axes and displaying the title. <bookmark mark='show_curve'/> Then, we plot the sigmoid curve, display its mathematical formula, and place a red dot at the origin.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Next, we animate the movement of the dot along the sigmoid curve to a new position.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1979", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene45(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate a \"gradient check\" comparison between analytical gradients and numerical gradients, showing tiny differences.\n        title = Title(\"Animate a \\\"gradient check\\\" comparison be\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid function, defined as sigma of x equals one over, one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Next, we animate the dot <bookmark mark='move_dot'/> as it moves along the curve to evaluate the function at a different point.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1948", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene168(VoiceoverScene):\n    def construct(self):\n        # Set up speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize a deep ResNet with 50 layers, where the skip connections are shown as jumping-over arrows.\n        title = Title(\"Visualize a deep ResNet with 50 layers, \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our environment, displaying the title and drawing the coordinate axes. \"\n                 \"<bookmark mark='show_curve'/> Next, we plot the sigmoid activation function, represented by the blue curve, \"\n                 \"along with its mathematical formula, sigma of x. We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Finally, we animate the red dot as it moves smoothly along the sigmoid curve to the right.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_1987", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene347(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the parameter iterator yielding all trainable tensors, highlighting each layer's parameters.\n        title = Title(\"Visualize the parameter iterator yieldin\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our title. <bookmark mark='show_graph'/> Next, we construct the coordinate axes, plot the sigmoid curve, and display its mathematical formula, sigma of x equals one over one plus e to the negative x. <bookmark mark='move_dot'/> Finally, we can trace the function's value by moving a point along the curve.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_graph\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2018", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene124(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize a character vocabulary as a color-coded legend, mapping each character (a-z, space, punctuation) to a unique color.\n        title = Title(\"Visualize a character vocabulary as a co\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function. \"\n            \"<bookmark mark='show_label'/> Here is the mathematical formula for the sigmoid function, \"\n            \"sigma of x equals one divided by the quantity one plus e to the negative x. \"\n            \"<bookmark mark='show_dot'/> We also place a red dot at the center where x is zero. \"\n            \"<bookmark mark='move_dot'/> Now, let us observe how the dot moves along the curve as x increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_2033", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene224(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the addition of the skip connection output and the main path output, merging two streams.\n        title = Title(\"Show the addition of the skip connection\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the function. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"We also place a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, let us observe how the output changes as we move \"\n                 \"the input along the curve towards the right.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2042", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene107(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize a \"gradient flow\" diagram using thick blue lines for strong gradients and thin red lines for vanishing gradients.\n        title = Title(\"Visualize a \\\"gradient flow\\\" diagram usin\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, along with its mathematical definition, sigma of x equals one divided by one plus e to the power of negative x. <bookmark mark='move_dot'/> Now, let's observe how a point moves along this curve, representing the gradient flow.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_1990", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene219(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the effect of momentum in SGD: a moving average of gradients represented by a trailing vector.\n        title = Title(\"Illustrate the effect of momentum in SGD\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes <bookmark mark='SHOW_CURVE'/> and plotting the sigmoid function, which represents our optimization landscape. <bookmark mark='SHOW_DOT'/> We also place a red dot at the starting position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"SHOW_CURVE\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"SHOW_DOT\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, watch how the parameter, represented by the red dot, moves smoothly along the curve as it updates.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2058", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene244(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the difference between causal and non-causal convolution using overlapping arrows.\n        title = Title(\"Illustrate the difference between causal\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one over one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Watch how the red dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2098", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene54(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the gradient of cross-entropy with respect to the logits as a vector field on the probability simplex.\n        title = Title(\"Visualize the gradient of cross-entropy \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function, plotting its characteristic S-shaped curve alongside its mathematical definition, sigma of x equals one over, one plus e to the negative x. <bookmark mark='move_dot'/> Let us observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(\n                Write(title), \n                Create(ax), \n                Create(curve), \n                Write(label), \n                FadeIn(dot)\n            )\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=2\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2138", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene18(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the effect of input normalization on subsequent layer activations using before/after histograms.\n        title = Title(\"Illustrate the effect of input normaliza\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us begin by setting up our coordinate system to analyze the activation behavior. \"\n                 \"<bookmark mark='show_sigmoid'/> Here, we plot the standard sigmoid activation function, \"\n                 \"defined as sigma of x equals one over one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Watch how the activation value changes as we shift the input along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2099", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene241(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the full forward pass from raw character IDs to final cross-entropy loss, highlighting each mathematical operation in sequence.\n        title = Title(\"Animate the full forward pass from raw c\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by displaying the title, <bookmark mark='show_elements'/> and then we set up our coordinate system to visualize the sigmoid function, defined as sigma of x equals one divided by the sum of one and e to the negative x. Let's plot the curve and place a point at the origin.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, watch as we transition the point along the curve to evaluate the function at x equals two.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_2116", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene42(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the flattening of a context window (e.g., 3 characters) into a single input vector for the MLP.\n        title = Title(\"Visualize the flattening of a context wi\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> and plotting the sigmoid activation function, represented by sigma of x. <bookmark mark='move_dot'/> Now, let us watch how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2142", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene53(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the kernel weights being applied to non-contiguous positions (gaps in the input).\n        title = Title(\"Show the kernel weights being applied to\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function. \"\n                 \"<bookmark mark='show_formula'/> This function is defined as sigma of x equals one divided by, \"\n                 \"one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_formula\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the sigmoid curve as the input value increases.\"\n        ) as tracker:\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))),\n                run_time=tracker.duration\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2158", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene300(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the batch sampling process: shuffling dataset indices, grouping them into mini-batches, and feeding them to the model.\n        title = Title(\"Animate the batch sampling process: shuf\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us begin by displaying the title. <bookmark mark='show_axes'/> Next, we set up our coordinate axes and plot the characteristic S-shaped curve. \"\n                 \"<bookmark mark='show_label'/> Here is the mathematical definition, where sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_axes\")\n            self.play(Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, watch as the dot moves along the curve towards the upper asymptote.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2194", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene118(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the training data as a sliding window over a text corpus, highlighting the current bigram pair.\n        title = Title(\"Visualize the training data as a sliding\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. We start by setting up our coordinate axes and plotting the characteristic S-shaped curve, defined as sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='dot_move'/> We can also track a point along this curve as it approaches its upper limit of one.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_2200", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene15(VoiceoverScene):\n    def construct(self):\n        # Setup speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the loss landscape as a 3D mountain range, and the gradients as downhill vectors.\n        title = Title(\"Visualize the loss landscape as a 3D mou\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid curve, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Watch as we track a point moving along this trajectory.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2193", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene149(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the `nn.Linear` module as a box containing a weight matrix and bias vector, with internal shape annotations.\n        title = Title(\"Visualize the `nn.Linear` module as a bo\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='SHOW_DOT'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"SHOW_DOT\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the sigmoid curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2212", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene112(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the gradient flow through a dropout layer: some streams are stochastically blocked (multiplied by 0).\n        title = Title(\"Illustrate the gradient flow through a d\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_label'/> This function is defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, <bookmark mark='move_dot'/> watch as the dot moves smoothly along the sigmoid curve towards the right.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2221", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene216(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the gradient update on a single embedding row, shifting its position in vector space.\n        title = Title(\"Animate the gradient update on a single \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, plotting the sigmoid activation function, and placing a point at the origin. <bookmark mark='update'/> Then, we animate the gradient update, shifting the position of our point along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"update\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2256", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene61(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the scaling factor `(1/N)` in the BN gradient, showing the averaging effect.\n        title = Title(\"Illustrate the scaling factor `(1/N)` in\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, defined as one over, one plus e to the negative x. <bookmark mark='dot_move'/> Watch as we trace a point along this curve to observe its activation value.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2238", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene26(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the parameter count calculation for each layer, displaying the total number of trainable parameters at the bottom.\n        title = Title(\"Visualize the parameter count calculatio\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> to visualize the sigmoid function, defined as sigma of x equals one divided by one plus e to the negative x. We also place a red dot at the center. <bookmark mark='move_dot'/> Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2283", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene181(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the hierarchy of convolutions capturing longer and longer contexts (like a pyramid).\n        title = Title(\"Animate the hierarchy of convolutions ca\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, defined as sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='move_dot'/> Let us observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2331", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene290(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a line chart of the standard deviation of activations per layer, with a reference line for ideal scaling (e.g., ~0.6 for Tanh).\n        title = Title(\"Show a line chart of the standard deviat\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by setting up our coordinate axes and plotting the sigmoid curve, along with its mathematical definition, sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='move_dot'/> We also place a red dot at the center. Now, watch how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2290", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene65(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the generation process: output at time T depends only on inputs \u2264 T (strictly left-context).\n        title = Title(\"Animate the generation process: output a\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, plotting the sigmoid function, and writing its mathematical definition, \"\n                 \"where sigma of x equals one divided by, one plus e to the power of negative x. \"\n                 \"<bookmark mark='move_dot'/> Now, let us observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2343", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene397(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Create a \"training health\" traffic light: Green (healthy), Yellow (warning), Red (NaN/divergence) based on loss and gradient metrics.\n        title = Title(\"Create a \\\"training health\\\" traffic light\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let's set up our coordinate axes <bookmark mark='show_elements'/> to visualize the sigmoid activation function, defined as sigma of x equals one divided by one plus e to the negative x, with a red marker at the center. <bookmark mark='move_dot'/> Now, we observe the marker as it moves along the curve to the right.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(\n                Write(title),\n                Create(ax),\n                Create(curve),\n                Write(label),\n                FadeIn(dot)\n            )\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))),\n                run_time=2\n            )\n        \n        self.wait(1)", "status": "success"}
{"id": "sample_2381", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene218(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the state transition graph of the bigram model, highlighting the active transition arrow during each step of generation.\n        title = Title(\"Animate the state transition graph of th\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, defined as sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='move_dot'/> Let's place a dot at the center and watch it transition along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.time_remaining())\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2385", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene351(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the gradient flow through a deep network (10 layers) with no BN, comparing layer 1 vs. layer 10 gradient magnitudes.\n        title = Title(\"Show the gradient flow through a deep ne\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function. <bookmark mark='show_label'/> Here is the mathematical formula for sigma of x. <bookmark mark='move_dot'/> Now, watch how the red dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2420", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene145(VoiceoverScene):\n    def construct(self):\n        # Setup voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the entire ML project lifecycle: Data Collection \u2192 Preprocessing \u2192 Model Training \u2192 Evaluation \u2192 Deployment (DevOps pipe).\n        title = Title(\"Visualize the entire ML project lifecycl\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"In this visualization, we will explore the sigmoid function, which is commonly used in machine learning. \"\n            \"<bookmark mark='show_axes'/> Let's set up our coordinate axes, plot the sigmoid curve, display its mathematical formula, and place a point at the center. \"\n            \"<bookmark mark='move_dot'/> Now, watch how the point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_axes\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_2425", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene304(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the manual computation of gradients w.r.t gamma and beta using summations over the batch.\n        title = Title(\"Illustrate the manual computation of gra\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, defined as sigma of x equals one divided by the sum of one and e to the negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2483", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene256(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Compare ReLU and Tanh side-by-side using two identical MLPs, highlighting the dead ReLU neurons with static black bars.\n        title = Title(\"Compare ReLU and Tanh side-by-side using\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We start by setting up our coordinate axes and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_label'/> Here is its mathematical formula, sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let's observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2491", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene148(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate gamma scaling: multiplying the normalized output by gamma, stretching or compressing the distribution.\n        title = Title(\"Animate gamma scaling: multiplying the n\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='move_dot'/> Let's trace a point along this curve to see how its value changes as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2434", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene203(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create a \"capacity\" slider animation, increasing model parameters and watching the dev error form a U-shape (bias-variance tradeoff).\n        title = Title(\"Create a \\\"capacity\\\" slider animation, in\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2510", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene180(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the difference between maintaining variance (`sqrt(n)`) and not maintaining it (`1`) using two parallel training runs.\n        title = Title(\"Visualize the difference between maintai\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by setting up our coordinate axes <bookmark mark='show_curve'/> and plotting the sigmoid curve, defined as one over one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"<bookmark mark='move_dot'/> Now, watch as the dot moves along the curve towards the right, demonstrating how the output approaches one as the input increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2539", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene27(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Keep original geometric objects and coordinates\n        title = Title(\"Animate the weights histogram across epo\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        # Narrate and animate with precise bookmark synchronization\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, \"\n                 \"defined as sigma of x equals one divided by, one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Let's observe how a point moves along this activation curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2531", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene377(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize a \"docs\" page with a search bar, highlighting the API reference for a specific function.\n        title = Title(\"Visualize a \\\"docs\\\" page with a search ba\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid function, defined as sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='MOVE_DOT'/> Let's observe how a point on the curve moves as x increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"MOVE_DOT\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n        \n        self.wait(1)", "status": "success"}
{"id": "sample_2560", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene154(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the gradient flow through 30 layers with residual connections (stable) vs. without (vanishing).\n        title = Title(\"Animate the gradient flow through 30 lay\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Let's observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2525", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene311(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create a 3D surface plot of the log-likelihood function for a single bigram probability parameter (p).\n        title = Title(\"Create a 3D surface plot of the log-like\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"In this visualization, we explore the sigmoid activation function. \"\n                 \"<bookmark mark='show_axes'/> First, let us set up our coordinate axes and plot the sigmoid curve, \"\n                 \"<bookmark mark='show_label'/> which is defined as sigma of x equals one divided by the sum of one and e to the power of negative x. We also place a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_axes\")\n            self.play(Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2567", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene252(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the effect of momentum: the optimizer ball rolling down the slope, gaining speed.\n        title = Title(\"Show the effect of momentum: the optimiz\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. <bookmark mark='show_elements'/> Here is the curve, defined as sigma of x equals one divided by, one plus e to the power of minus x, along with a red optimizer ball. <bookmark mark='move_dot'/> Watch how the ball rolls along the slope, simulating the optimization process.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2574", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene161(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the \"internal covariate shift\" as the input distribution to a layer shifting epoch by epoch (moving histograms).\n        title = Title(\"Visualize the \\\"internal covariate shift\\\"\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the coordinate axes for our activation function, <bookmark mark='show_curve'/> plotting the sigmoid curve defined as sigma of x equals one divided by one plus e to the negative x, <bookmark mark='show_dot'/> along with an initial input point.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"As the network trains, the distribution of inputs shifts, causing the activation values to move along the curve.\"\n        ) as tracker:\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))),\n                run_time=tracker.duration\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2583", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene55(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the gradient sparsity in the embedding table (only a few rows updated per batch).\n        title = Title(\"Visualize the gradient sparsity in the e\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_elements'/> to visualize the sigmoid activation function, represented by sigma of x equals one over one plus e to the negative x. <bookmark mark='move_dot'/> Watch how the red dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2614", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene153(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Original objects\n        title = Title(\"Illustrate the effect of dropout as a ma\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        # First voiceover block: introducing the axes, curve, formula, and dot\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='show_formula'/> defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_formula\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        # Second voiceover block: moving the dot along the curve\n        with self.voiceover(\n            text=\"Now, let us observe how the output value changes as we move the input along the curve towards the right.\"\n        ) as tracker:\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))),\n                run_time=tracker.duration\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2622", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene167(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate an embedding table as a 2D grid (vocab_size x embedding_dim), looking up a character by highlighting its row vector.\n        title = Title(\"Animate an embedding table as a 2D grid \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the power of negative x. \"\n                 \"<bookmark mark='move_dot'/> Now, we observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2652", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene305(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the data flow inside a forward hook and backward hook, intercepting activations and gradients.\n        title = Title(\"Animate the data flow inside a forward h\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> defined as sigma of x equals one divided by the quantity one plus e to the power of negative x. \"\n                 \"<bookmark mark='move_dot'/> Let's track a data point as it flows through this activation function, moving along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_2627", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene344(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the reduced dependence on dropout, showing BN's regularizing effect lowering validation loss.\n        title = Title(\"Illustrate the reduced dependence on dro\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid activation function, \"\n                 \"<bookmark mark='show_sigmoid'/> defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2660", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene1(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the gradient flowing through a linear layer as a matrix multiplication, with shapes displayed.\n        title = Title(\"Visualize the gradient flowing through a\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid function, defined as sigma of x equals one divided by one plus e to the negative x, along with a red dot at the center. <bookmark mark='move_dot'/> Watch as the dot moves along the curve towards the right, demonstrating how the output approaches one as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2631", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene227(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the distance between input tokens that can interact at each layer, doubling each time.\n        title = Title(\"Illustrate the distance between input to\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the sigmoid function, defined as sigma of x equals one divided by, one plus e to the negative x, <bookmark mark='show_dot'/> along with a starting point at the center.\"\n        ) as tracker:\n            self.play(\n                Write(title),\n                Create(ax),\n                Create(curve),\n                Write(label)\n            )\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the point moves along the curve as the input x increases.\"\n        ) as tracker:\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))),\n                run_time=tracker.duration\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2675", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene2(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show a side-by-side comparison of a forward pass (green arrows) and a backward pass (red arrows) on a simple `sin(x) * y` graph.\n        title = Title(\"Show a side-by-side comparison of a forw\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, plotting the sigmoid activation function, and placing an initial point at the center. <bookmark mark='move_dot'/> Next, we watch the point transition along the curve to its new position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2689", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene199(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the manual gradient check by perturbing inputs and comparing to the analytical gradient.\n        title = Title(\"Animate the manual gradient check by per\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"To perform a manual gradient check, we can visualize how a function changes as we perturb its input. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid function, defined as sigma of x equals one over one plus e to the negative x, and place a point at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, we perturb the input by moving the point along the curve to observe the change in its value.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_2685", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene50(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the complex formula for `dL/dx` in BN expanded into a large equation on the screen.\n        title = Title(\"Show the complex formula for `dL/dx` in \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by displaying the title. <bookmark mark='axes'/> Next, we plot the sigmoid curve on our coordinate axes, <bookmark mark='label'/> and write its mathematical definition: sigma of x equals one divided by the quantity, one plus e to the power of negative x. <bookmark mark='dot'/> Finally, we place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"axes\")\n            self.play(Create(ax), Create(curve))\n            self.wait_until_bookmark(\"label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the sigmoid curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_2690", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene367(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the generation process of a bigram model, sequentially revealing characters one by one, with arrows indicating transition probabilities from the current character.\n        title = Title(\"Animate the generation process of a bigr\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let's visualize the sigmoid function. First, we set up our coordinate axes <bookmark mark='show_curve'/> and plot the sigmoid curve, defined as sigma of x equals one divided by one plus e to the negative x. We also place a red dot at the center. <bookmark mark='move_dot'/> Now, watch as the dot moves along the curve to show how the function's value changes as x increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_2719", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene207(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the receptive field growing like a binary tree as depth increases (two leaves merge).\n        title = Title(\"Animate the receptive field growing like\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one over, one plus e to the negative x. \"\n                 \"<bookmark mark='MOVE_DOT'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"MOVE_DOT\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2724", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene395(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the manual gradient contribution from the mean and variance terms separately.\n        title = Title(\"Visualize the manual gradient contributi\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system. <bookmark mark='show_curve'/> Next, we plot the sigmoid function, defined as one divided by one plus e to the negative x. <bookmark mark='move_dot'/> Watch how the red dot moves along the curve as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), FadeIn(dot))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2731", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene46(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the `named_parameters()` iterator traversing the tree, yielding parameter names and tensors.\n        title = Title(\"Show the `named_parameters()` iterator t\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes, plotting the sigmoid function, \"\n                 \"and placing a starting dot at the center. <bookmark mark='move_dot'/> \"\n                 \"Then, we watch the dot transition along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2737", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene166(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate a heatmap of the gradient matrix for a linear layer, showing which weights receive the largest updates.\n        title = Title(\"Animate a heatmap of the gradient matrix\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_label'/> The function is defined as sigma of x equals one over one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, watch as the dot <bookmark mark='move_dot'/> moves along the curve to show how the activation value changes as x increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_2728", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene376(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the concept of \"flat minima\" by showing a wide valley vs. a sharp spike, both with low loss.\n        title = Title(\"Illustrate the concept of \\\"flat minima\\\" \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by the sum of one and e to the negative x. \"\n                 \"<bookmark mark='dot_move'/> Let us track a point moving along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2747", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene193(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the gradient descent update rule `w = w - lr * dw` as a vector moving a point on a parabolic surface.\n        title = Title(\"Animate the gradient descent update rule\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by setting up our coordinate axes <bookmark mark='show_elements'/> and plotting the sigmoid curve, defined as sigma of x equals one divided by, one plus e to the power of negative x. We also place a red dot at the center. <bookmark mark='move_dot'/> Now, we can animate the movement of this point along the curve to a new position.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2748", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene313(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the \"temperature\" parameter scaling logits: high T (flat pie), low T (dominating slice), and T=1 (standard).\n        title = Title(\"Visualize the \\\"temperature\\\" parameter sc\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us begin by plotting the sigmoid function on our coordinate axes, <bookmark mark='show_elements'/> starting with a red dot at the center where the input is zero.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n\n        with self.voiceover(\n            text=\"As we increase the input value, <bookmark mark='move_dot'/> the dot slides along the curve, approaching an output value of one.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_2752", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene399(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the effect of embedding dimension size: small dim (underfit) vs. large dim (overfit) on the loss curve.\n        title = Title(\"Illustrate the effect of embedding dimen\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's explore the behavior of a standard activation function. We begin by setting up our coordinate axes, plotting the sigmoid curve, and placing a reference point at the center. <bookmark mark='move_dot'/> Now, we can observe how this point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2853", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene127(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize Xavier initialization: scaling weights by `sqrt(1/fan_in)` to maintain variance of 1 across layers.\n        title = Title(\"Visualize Xavier initialization: scaling\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"To understand Xavier initialization, we look at how inputs scale through activation functions like the sigmoid. Let us first plot the sigmoid function and its formula, <bookmark mark='show_dot'/> placing a starting point at the origin. <bookmark mark='move_dot'/> Now, watch how the point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2784", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene209(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the parameter buffer registration (`register_buffer`) for non-trainable tensors (e.g., running_mean).\n        title = Title(\"Show the parameter buffer registration (\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function, which maps any real-valued number into a range between zero and one. \"\n                 \"<bookmark mark='move_dot'/> We can observe how a point moves along this curve as the input value increases, asymptotically approaching one.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2855", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene37(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Create a \"weight explosion\" animation where random weights initialize and the activation statistics are computed.\n        title = Title(\"Create a \\\"weight explosion\\\" animation wh\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, defined as sigma of x equals one divided by, one plus e to the power of negative x. <bookmark mark='MOVE_DOT'/> Let's observe how the activation value changes as we move along the curve towards the positive direction.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"MOVE_DOT\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2869", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene19(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the `zero_grad()` method iterating over all parameters and setting their grad to None.\n        title = Title(\"Illustrate the `zero_grad()` method iter\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, defined as sigma of x equals one divided by one plus e to the negative x. <bookmark mark='dot_move'/> Watch as we track a point along this curve, moving from the center towards the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2856", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene306(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the gradient accumulation process during backpropagation, showing how partial derivatives sum when a variable is used in multiple branches.\n        title = Title(\"Animate the gradient accumulation proces\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the sigmoid function, which is commonly used in neural networks. <bookmark mark='show_elements'/> Here we have the coordinate axes, the sigmoid curve, and its mathematical definition: sigma of x equals one divided by the quantity one plus e to the negative x.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(\n                Create(ax),\n                Create(curve),\n                Write(label),\n                FadeIn(dot)\n            )\n            \n        with self.voiceover(\n            text=\"<bookmark mark='move_dot'/> Now, let's observe how a point moves along this activation curve as its input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))),\n                run_time=tracker.duration\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2885", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene25(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the main training loop as an infinite cycle: Forward \u2192 Loss \u2192 Backward \u2192 Optimizer step, with a rotating progress wheel.\n        title = Title(\"Visualize the main training loop as an i\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='plot_curve'/> and plotting the sigmoid function, defined as sigma of x equals one divided by the quantity one plus e to the power of negative x. <bookmark mark='move_dot'/> Let's place a point at the center, and then trace its path as it moves along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"plot_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2890", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene323(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the \"causal\" constraint: the convolution only looks at past tokens (masked future tokens).\n        title = Title(\"Illustrate the \\\"causal\\\" constraint: the \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Next, we animate the dot as it moves along the curve to the right.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2892", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene129(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the evolution of embeddings over time using a t-SNE animation, where character clusters gradually separate.\n        title = Title(\"Show the evolution of embeddings over ti\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid function, \"\n                 \"<bookmark mark='show_curve'/> which maps any real-valued number to a value between zero and one. \"\n                 \"<bookmark mark='move_dot'/> Let us observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            \n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2913", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene270(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the manual calculation of `dL/dw` for a single weight using the finite difference method, comparing it to the analytical gradient.\n        title = Title(\"Animate the manual calculation of `dL/dw\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> which is defined as sigma of x equals one over one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a starting point on the curve at x equals zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, we animate the movement of this point along the curve to x equals two, \"\n                 \"demonstrating how the function value changes as we increase the input.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2905", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene233(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the difference between NLL and cross-entropy using two overlapping integral curves.\n        title = Title(\"Illustrate the difference between NLL an\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the sigmoid activation function. <bookmark mark='axes'/> First, we set up our axes. <bookmark mark='curve'/> Next, we plot the sigmoid curve, defined as sigma of x equals one over one plus e to the negative x. <bookmark mark='dot'/> We also place a point on the curve.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"axes\")\n            self.play(Create(ax))\n            self.wait_until_bookmark(\"curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, we can observe how the point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2903", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene104(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize a rank-0 scalar, rank-1 vector, rank-2 matrix, and rank-3 tensor as expanding 3D geometric blocks.\n        title = Title(\"Visualize a rank-0 scalar, rank-1 vector\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_formula'/> Here is its mathematical formula, sigma of x equals one divided by \"\n                 \"the quantity one plus e to the power of negative x. <bookmark mark='show_dot'/> We also place a red dot \"\n                 \"at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_formula\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, watch as the dot moves along the sigmoid curve, transitioning towards the upper asymptote.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_2922", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene380(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Define the visual elements\n        title = Title(\"Animate the split of the convolution out\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        # Narrated animation sequence\n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function. \"\n                 \"<bookmark mark='show_graph'/> Here, we plot the curve and display its mathematical formula, \"\n                 \"sigma of x equals one divided by the sum of one and e to the power of negative x. \"\n                 \"We also place a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, watch as the dot moves along the curve, \"\n                 \"demonstrating how the function maps input values to outputs between zero and one.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_graph\")\n            self.play(\n                Write(title), \n                Create(ax), \n                Create(curve), \n                Write(label), \n                FadeIn(dot)\n            )\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=2\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2949", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene75(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the difference between `retain_graph=True` and `retain_graph=False` with a reference counter decreasing to zero.\n        title = Title(\"Illustrate the difference between `retai\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, plotting the sigmoid function, and placing a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, let us observe the movement of this dot along the sigmoid curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2982", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene336(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the backward pass through the log-softmax combination, canceling terms to simplify.\n        title = Title(\"Visualize the backward pass through the \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the backward pass through the log-softmax combination, \"\n                 \"canceling terms to simplify. Let's set up our coordinate axes, plot the sigmoid \"\n                 \"activation function, and place a point on the curve. <bookmark mark='move_dot'/> \"\n                 \"Now, we can observe how this point moves along the trajectory of the sigmoid function.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2963", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene106(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the probability mass moving from the wrong class to the correct class as training progresses.\n        title = Title(\"Animate the probability mass moving from\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> At the center, we place a red dot representing our initial probability mass. \"\n                 \"<bookmark mark='move_dot'/> As training progresses, we animate this probability mass moving along the curve towards the correct class.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2968", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene85(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a flow diagram of the training loop: gather counts \u2192 normalize rows \u2192 sample \u2192 compute loss.\n        title = Title(\"Show a flow diagram of the training loop\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> defined as sigma of x equals one divided by, one plus e to the power of negative x. \"\n                 \"<bookmark mark='move_dot'/> Let us observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2983", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene39(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the distribution of outputs after softmax: high entropy (flat) vs low entropy (spiky).\n        title = Title(\"Illustrate the distribution of outputs a\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let's visualize the sigmoid function, which maps any real-valued number to a value between zero and one. \"\n                 \"Here is the plot of the sigmoid function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Watch how the output value approaches one as the input increases.\"\n         ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2989", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene156(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the probability mass distribution over the next character for a given input character (e.g., 'h'), shown as a horizontal bar chart.\n        title = Title(\"Visualize the probability mass distribut\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, defined as sigma of x equals one divided by the sum of one and e to the negative x. <bookmark mark='move_dot'/> Let us observe how a point on this curve moves as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2997", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene115(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a decision tree of broadcasting rules: Check dims \u2192 Align right \u2192 Expand size 1 \u2192 Check compatibility.\n        title = Title(\"Show a decision tree of broadcasting rul\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function, defined as sigma of x equals one divided by one plus e to the power of negative x. <bookmark mark='show_elements'/> We set up our coordinate axes, plot the characteristic S-shaped curve, and place a marker at the center. <bookmark mark='move_dot'/> Watch how the marker moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_2992", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene195(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the layer-wise learning rate decay, where earlier layers get smaller updates.\n        title = Title(\"Animate the layer-wise learning rate dec\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by the sum of one and e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Now, let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3003", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene212(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the forward pass with untuned scaling: activations growing exponentially deeper, eventually hitting NaN.\n        title = Title(\"Visualize the forward pass with untuned \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the forward pass with untuned scaling. \"\n                 \"Here, we set up our coordinate axes, <bookmark mark='show_curve'/> plot the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by one plus e to the power of negative x, \"\n                 \"<bookmark mark='show_dot'/> and place an initial activation point on the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the activation value changes <bookmark mark='move_dot'/> as we transition along the curve.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3038", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene289(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the softmax output as a pie chart, with slices representing probabilities of each character.\n        title = Title(\"Show the softmax output as a pie chart, \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"First, we display the title and set up our coordinate axes. <bookmark mark='show_curve'/> Then, we plot the sigmoid curve, write its mathematical formula, and place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Next, we watch the red dot move smoothly along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3039", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene390(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create an animation of tensor reshaping (e.g., 2x3 to 3x2) as a grid re-arranging its elements.\n        title = Title(\"Create an animation of tensor reshaping \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the sigmoid activation function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. We start with a point at the input zero, where the output is point five. <bookmark mark='move_dot'/> As we increase the input value, the point moves along the curve, asymptotically approaching one.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3103", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene396(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the derivation of the gradient of Cross-Entropy loss w.r.t logits: `dL/dz = softmax(z) - y`.\n        title = Title(\"Animate the derivation of the gradient o\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this activation curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_3102", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene163(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the derivative chain for the standard deviation term, highlighting the `1/sqrt(var)` factor.\n        title = Title(\"Animate the derivative chain for the sta\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, sigma of x, which is defined as one divided by the sum of one and e to the power of negative x. <bookmark mark='move_dot'/> Next, we can trace the value along the curve by moving our red point to the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3126", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene96(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the gradient vector (p - y) pointing to increase the probability of the correct class and decrease the others.\n        title = Title(\"Show the gradient vector (p - y) pointin\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function, which maps inputs to probabilities. \"\n                 \"<bookmark mark='show_elements'/> Here, we display the coordinate axes, the sigmoid curve, \"\n                 \"its mathematical formula, and a starting point on the curve. \"\n                 \"<bookmark mark='move_dot'/> Next, we observe how the point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3088", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene301(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create a side-by-side comparison of the initial random output distribution and the final output distribution after training (softmax probabilities).\n        title = Title(\"Create a side-by-side comparison of the \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> to plot the sigmoid function, defined as sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='show_dot'/> We also place an initial point at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the output value changes as we move the input along the horizontal axis, tracing the sigmoid curve towards the right.\"\n        ) as tracker:\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))),\n                run_time=tracker.duration\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3127", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene231(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize a 1D convolutional filter sliding over a sequence of character embeddings.\n        title = Title(\"Visualize a 1D convolutional filter slid\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the curve, display its mathematical formula, \"\n                 \"sigma of x equals one divided by one plus e to the negative x, and place a point at the origin. \"\n                 \"<bookmark mark='move_dot'/> Watch how the point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3146", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene258(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the pre-activation distribution (histogram) before and after BatchNorm in a hidden layer.\n        title = Title(\"Visualize the pre-activation distributio\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, defined as sigma of x equals one divided by, one plus e to the power of negative x. <bookmark mark='show_dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us watch the dot move along the curve as the input value increases to two.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3169", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene174(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the manual calculation of gradients for a 3-layer MLP, showing the nested chain rule expansion.\n        title = Title(\"Animate the manual calculation of gradie\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid activation function, which is crucial for calculating gradients in a multi-layer perceptron. <bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as one divided by, one plus e to the power of negative x. <bookmark mark='move_dot'/> Now, let us observe how a point moves along this curve as we evaluate its value at a different input.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_3156", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene23(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize a dilation rate of 2: the filter skips every other input, effectively spanning a wider context.\n        title = Title(\"Visualize a dilation rate of 2: the filt\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> defined as sigma of x equals one over one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve <bookmark mark='move_dot'/> as we increase the input value to two.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3192", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene263(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the receptive field as a colored window expanding over the input sequence.\n        title = Title(\"Visualize the receptive field as a color\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let's visualize the function. We set up our coordinate axes <bookmark mark='show_curve'/> and plot the sigmoid function, defined as sigma of x equals one divided by one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the point where x is zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let's observe how the dot moves along the curve as we increase the input value x.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_3175", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene295(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a before/after comparison of layer input distributions over 10 epochs: without BN (drifting), with BN (stationary).\n        title = Title(\"Show a before/after comparison of layer \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize how a layer's input is transformed. We start by setting up our coordinate axes and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_label'/> This function is defined as sigma of x equals one divided by the quantity of one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot representing an initial input value. \"\n                 \"<bookmark mark='move_dot'/> Now, watch as the input value increases, and the dot moves smoothly along the sigmoid curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            \n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            \n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3203", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene70(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the backward pass through a `+` node, demonstrating how the upstream gradient is split equally to both children.\n        title = Title(\"Animate the backward pass through a `+` \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by setting up our coordinate axes, <bookmark mark='show_curve'/> then we plot the sigmoid curve and place a starting point at the center. <bookmark mark='move_dot'/> Now, watch as the point moves along the curve to evaluate the function at a higher input value.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3231", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene370(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize a single scalar neuron with inputs `x1, x2`, weights `w1, w2`, and bias `b`, animating the weighted sum and the subsequent tanh activation.\n        title = Title(\"Visualize a single scalar neuron with in\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation curve. \"\n                 \"<bookmark mark='show_label'/> This curve is defined by the function, sigma of x, equals one divided by, one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> Let's place a tracking point on the curve at x equals zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, watch how the point moves along the curve as the input value increases to two.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3259", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene102(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Display a scalar-valued loss function (e.g., MSE) as a 2D curve, and highlight the specific point where the gradient is computed via backprop.\n        title = Title(\"Display a scalar-valued loss function (e\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by displaying a scalar-valued loss function as a two-dimensional curve, \"\n                 \"with the sigmoid formula shown in the corner. <bookmark mark='show_dot'/> We also highlight a specific point \"\n                 \"on the curve. <bookmark mark='move_dot'/> Now, we trace this point along the curve to evaluate the function \"\n                 \"at a different input value.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_3299", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene297(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the evolution of activations from epoch 1 to epoch 100 using a violin plot that widens/narrows.\n        title = Title(\"Visualize the evolution of activations f\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid activation function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='move_dot'/> Watch how the activation value, represented by the red dot, increases as we move to the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3274", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene35(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a table of intermediate variables (a, b, c) and their gradients, updating row-by-row during backward pass.\n        title = Title(\"Show a table of intermediate variables (\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, <bookmark mark='label_and_dot'/> which is defined as sigma of x equals one over one plus e to the negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"label_and_dot\")\n            self.play(Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3341", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene242(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize a compute graph and ask the user to trace gradients, revealing the answer with a color fade.\n        title = Title(\"Visualize a compute graph and ask the us\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> and plotting the sigmoid function, defined as sigma of x equals one divided by, one plus e to the power of negative x. <bookmark mark='show_dot'/> We also place a red dot at the center. <bookmark mark='move_dot'/> Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3354", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene130(VoiceoverScene):\n    def construct(self):\n        # Initialize voiceover service\n        self.set_speech_service(\n            AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\")\n        )\n\n        # Animate the `detach()` operation, severing a tensor from its computation history, turning the edge from solid to dashed.\n        title = Title(\"Animate the `detach()` operation, severi\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, plotting the sigmoid function, and placing a starting point at the center. <bookmark mark='move_dot'/> Next, we watch the point transition smoothly along the curve to its new position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3370", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene223(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create an animation of the \"dying ReLU\" where a neuron's output remains zero regardless of input (flat line).\n        title = Title(\"Create an animation of the \\\"dying ReLU\\\" \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"represented by sigma of x equals one divided by one plus e to the power of negative x. \"\n                 \"<bookmark mark='move_dot'/> Let's observe how the output changes as we move a point along this curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3390", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene64(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize a shape mismatch error with red flashing, followed by the automatic expansion of dimensions during broadcasting.\n        title = Title(\"Visualize a shape mismatch error with re\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our environment, <bookmark mark='show_all'/> displaying the axes, the sigmoid curve, its mathematical formula, and a red dot at the origin.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_all\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Next, we animate the red dot <bookmark mark='move_dot'/> as it moves smoothly along the sigmoid trajectory.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3400", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene72(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create a block diagram of the evaluation pipeline: Model \u2192 Generate \u2192 Compare to Ground Truth \u2192 Compute Metrics.\n        title = Title(\"Create a block diagram of the evaluation\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> to plot the sigmoid activation function, defined as sigma of x equals one over one plus e to the negative x. <bookmark mark='show_dot'/> We also place a point at the center where x is zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3401", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene186(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the Hessian and curvature: gradients pointing in a narrow canyon causing oscillations.\n        title = Title(\"Illustrate the Hessian and curvature: gr\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the curvature of a function. We set up our coordinate axes, plot the sigmoid curve, and place a red dot at the center. <bookmark mark='move_dot'/> Watch as the dot moves smoothly along the curve to its new position, demonstrating the gradient's path.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3415", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene146(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the manual computation of the Tanh gradient for a batch of inputs, element-wise.\n        title = Title(\"Illustrate the manual computation of the\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid function, \"\n                 \"defined as sigma of x equals one divided by the quantity of one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Now, let's observe how a point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3410", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene352(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the verification of manual BN gradients against autograd using a numeric checker.\n        title = Title(\"Animate the verification of manual BN gr\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_label'/> The function is defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center, representing our initial point.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us animate the movement of this dot along the curve <bookmark mark='move_dot'/> to observe how the sigmoid function approaches its upper asymptote of one.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_3421", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene210(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the forward pass through the MLP: Input \u2192 Linear1 \u2192 Tanh \u2192 Linear2 \u2192 Logits, with dimension sizes displayed between layers.\n        title = Title(\"Show the forward pass through the MLP: I\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function, which maps any real-valued number to a value between zero and one. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the curve, display its mathematical formula, and place a starting point. \"\n                 \"<bookmark mark='move_dot'/> Now, let us observe how the point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3485", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene280(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the distribution shift between splits, using overlapping histograms of character frequencies.\n        title = Title(\"Visualize the distribution shift between\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes, <bookmark mark='plot_curve'/> and then plotting the sigmoid function. <bookmark mark='show_label'/> This is defined as sigma of x equals one over one plus e to the negative x. <bookmark mark='show_dot'/> Finally, we place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"plot_curve\")\n            self.play(Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the sigmoid curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3498", "narrated_manim_code": "from manim import *\nimport numpy as np\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\n\nclass AOSTrajectoryScene357(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the decision boundary of a 2D binary classification problem as the MLP trains, showing misclassified points shifting color.\n        title = Title(\"Animate the decision boundary of a 2D bi\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_label'/> The function is defined as sigma of x equals one divided by \"\n                 \"one plus e to the power of negative x. <bookmark mark='show_dot'/> We also place a starting \"\n                 \"point on the curve at x equals zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the point moves along the sigmoid curve <bookmark mark='move_dot'/> as its input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_3512", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene246(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the context size needed for a long-range dependency (e.g., matching parentheses).\n        title = Title(\"Illustrate the context size needed for a\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid function, \"\n                 \"defined as sigma of x equals one over, one plus e to the negative x. \"\n                 \"<bookmark mark='MOVE_DOT'/> Then, we can observe how a point moves along this \"\n                 \"activation curve as its input value increases.\"\n        ) as tracker:\n            self.play(\n                Write(title),\n                Create(ax),\n                Create(curve),\n                Write(label),\n                FadeIn(dot)\n            )\n            self.wait_until_bookmark(\"MOVE_DOT\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))),\n                run_time=2\n            )\n        self.wait(1)", "status": "success"}
{"id": "sample_3530", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene266(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the gradient flow as a cascade of shrinking waterfall bars descending through the layers.\n        title = Title(\"Show the gradient flow as a cascade of s\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our visualization. <bookmark mark='show_elements'/> Here, we write the title, create the coordinate axes, plot the sigmoid curve, display its mathematical formula, and place a red dot at the center. <bookmark mark='move_dot'/> Next, we animate the dot as it moves along the curve to the right.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3526", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene52(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate a confusion matrix for character classification, with correct predictions on the diagonal lighting up green.\n        title = Title(\"Animate a confusion matrix for character\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the sigmoid activation function. We plot the curve on a coordinate system, defined by the formula, sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the y-intercept.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, watch as the dot moves along the curve to the right, approaching a value of one as x increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3562", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene76(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the gradient distribution of the first layer widening after BN is applied.\n        title = Title(\"Show the gradient distribution of the fi\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by displaying the sigmoid function, which is defined as one over, one plus e to the negative x. \"\n                 \"<bookmark mark='show_curve'/> Here is the curve, along with a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, let us observe how the dot moves along the curve as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Write(label))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3538", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene184(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a confusion matrix comparing predicted next-character probabilities vs. actual next-character labels in the validation set.\n        title = Title(\"Show a confusion matrix comparing predic\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the sigmoid function, which maps any real value into a range between zero and one. <bookmark mark='show_elements'/> Here we have our coordinate axes, the sigmoid curve, its mathematical formula, and a point starting at the origin.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, <bookmark mark='move_dot'/> watch as the point moves along the curve, asymptotically approaching one as the input increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3610", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene245(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the backpropagation path through the MLP, highlighting the gradient updates on each linear layer.\n        title = Title(\"Illustrate the backpropagation path thro\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid activation function. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as one divided by, \"\n                 \"one plus e to the power of negative x, and place a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, we observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3593", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene386(VoiceoverScene):\n    def construct(self):\n        # Set up speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        title = Title(\"Show a table comparing receptive field f\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let's visualize the sigmoid activation function. We plot the function on a set of coordinate axes, <bookmark mark='label'/> defined by the formula sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let's observe how the dot moves along the curve <bookmark mark='move'/> as we increase the input value x towards two.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_3614", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene183(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the `apply()` function recursively applying a function (e.g., weight init) to all modules.\n        title = Title(\"Visualize the `apply()` function recursi\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let's visualize the sigmoid activation function. We begin by setting up our coordinate axes, \"\n                 \"<bookmark mark='show_curve'/> plotting the sigmoid curve, and writing its mathematical definition: \"\n                 \"sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='show_dot'/> \"\n                 \"We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"<bookmark mark='move_dot'/> Watch as the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_3622", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene109(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate a grid search over LR and weight decay, with each point's final loss represented as color on a heatmap.\n        title = Title(\"Animate a grid search over LR and weight\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_label'/> This function is defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We place a red dot at the center of the curve. \"\n                 \"<bookmark mark='move_dot'/> Finally, we animate the dot as it moves along the curve towards the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3632", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene111(VoiceoverScene):\n    def construct(self):\n        # Initialize voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize BatchNorm as a \"processing block\" inserted between Linear and Activation, normalizing the distribution.\n        title = Title(\"Visualize BatchNorm as a \\\"processing blo\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, <bookmark mark='show_dot'/> along with a reference point at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how this point moves along the sigmoid curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_3639", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene110(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the effect of the Tanh derivative on the gradient signal: attenuating large inputs.\n        title = Title(\"Visualize the effect of the Tanh derivat\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the effect of the activation function. We begin by plotting the sigmoid curve, defined as sigma of x equals one divided by the quantity one plus e to the power of negative x. <bookmark mark='move_dot'/> Watch how the red dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3644", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene316(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the cross-entropy surface for a 2-class problem as a 3D terrain, with the gradient descent path.\n        title = Title(\"Show the cross-entropy surface for a 2-c\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_sigmoid'/> to visualize the sigmoid activation function, defined as sigma of x equals one over one plus e to the negative x.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3654", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene298(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the storage layout (contiguous memory) of a tensor in row-major order using a strip of colored cells.\n        title = Title(\"Show the storage layout (contiguous memo\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, <bookmark mark='show_label'/> defined as sigma of x equals one over one plus e to the negative x.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how a point <bookmark mark='move_dot'/> moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3661", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene82(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show a PyTorch `BatchNorm1d` module diagram, including buffers for running_mean and running_var.\n        title = Title(\"Show a PyTorch `BatchNorm1d` module diag\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid function. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as sigma of x \"\n                 \"equals one divided by the sum of one and e to the power of negative x. We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n\n        with self.voiceover(\n            text=\"<bookmark mark='move_dot'/> Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_3678", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene341(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the NLL loss per sample as a bar chart, with high-loss samples highlighted in red.\n        title = Title(\"Visualize the NLL loss per sample as a b\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, plotting the sigmoid function, and placing a red dot at the center. <bookmark mark='move_dot'/> Then, we animate the dot as it moves along the curve to the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3685", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene98(VoiceoverScene):\n    def construct(self):\n        # Setup voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the running mean/variance converging over time, becoming stable by the end of training.\n        title = Title(\"Visualize the running mean/variance conv\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function, which is often used to map inputs to probabilities. \"\n                 \"<bookmark mark='show_elements'/> We plot the curve along with its mathematical formula, \"\n                 \"sigma of x equals one over one plus e to the negative x, and place a tracker dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Watch as the dot moves along the curve towards its asymptotic limit.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3679", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene385(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the training loss for WaveNet with different depths (10, 20, 30 layers) converging.\n        title = Title(\"Visualize the training loss for WaveNet \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes. \"\n                 \"<bookmark mark='show_curve'/> Next, we plot the sigmoid curve, \"\n                 \"defined by the formula sigma of x equals one divided by the quantity \"\n                 \"one plus e to the negative x, and place a starting point at the center. \"\n                 \"<bookmark mark='move_dot'/> Finally, we animate the point as it moves \"\n                 \"along the curve towards the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3718", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene120(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the running mean and running variance updating via exponential moving average (smoothing).\n        title = Title(\"Visualize the running mean and running v\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function. \"\n                 \"<bookmark mark='show_label'/> Here is its mathematical formula, with a starting point at zero. \"\n                 \"<bookmark mark='move_dot'/> Now, we watch the point transition smoothly along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3703", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene220(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize \"internal covariate shift\" as a drifting distribution of inputs to a layer, and BN as a clamp holding it steady.\n        title = Title(\"Visualize \\\"internal covariate shift\\\" as \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"To understand internal covariate shift, let us first set up our coordinate system <bookmark mark='show_sigmoid'/> and plot the sigmoid activation function, represented by sigma of x, along with an initial input value.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"As the parameters of the previous layers change during training, the distribution of inputs shifts, <bookmark mark='move_dot'/> causing our input value to drift along the activation curve.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3717", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene190(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the reduction of gradient vanishing in early layers: arrows become visibly stronger after adding BN.\n        title = Title(\"Animate the reduction of gradient vanish\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"To understand how we can reduce gradient vanishing, let us first visualize the standard sigmoid activation function. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve along with its mathematical definition, and place a point at the center. \"\n                 \"<bookmark mark='move_dot'/> As we move along the curve, we can observe how the gradient changes.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3778", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene56(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create a sliding window animation of a context buffer shifting left as new tokens are generated.\n        title = Title(\"Create a sliding window animation of a c\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. We start by setting up our coordinate axes, plotting the characteristic S-shaped curve, and placing a red dot at the center. <bookmark mark='move_dot'/> Now, watch as the dot slides along the curve towards the upper asymptote.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3774", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene273(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the effect of gating on long-range dependencies: allowing only relevant signals to pass.\n        title = Title(\"Animate the effect of gating on long-ran\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"To understand how gating affects long range dependencies, we plot the sigmoid activation function, <bookmark mark='MOVE_DOT'/> and observe how a signal moves along the curve as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"MOVE_DOT\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3805", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene47(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the creation of `gamma` and `beta` as tensors of shape (num_features), initialized to 1 and 0.\n        title = Title(\"Animate the creation of `gamma` and `bet\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, defined as sigma of x equals one over one plus e to the negative x. <bookmark mark='move_dot'/> Let us now trace the curve by moving our red dot along the trajectory.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3809", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene116(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a 2D contour plot of a loss surface, and animate the gradient vector at multiple points pointing in the direction of steepest ascent.\n        title = Title(\"Show a 2D contour plot of a loss surface\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, defined as sigma of x equals one divided by one plus e to the negative x. <bookmark mark='dot_move'/> Now, watch as we trace a point along this curve, moving it to the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3815", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene34(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the model's confidence score as a thermometer rising or falling for each prediction.\n        title = Title(\"Illustrate the model's confidence score \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"To illustrate the model's confidence score, we first set up our coordinate axes and write the title. \"\n                 \"<bookmark mark='curve'/> Next, we plot the sigmoid activation function, which maps inputs to a probability between zero and one. \"\n                 \"<bookmark mark='label'/> We also display the mathematical formula for the sigmoid function, \"\n                 \"<bookmark mark='dot'/> and place a red dot at the initial prediction value of zero point five.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"curve\")\n            self.play(Create(curve))\n            self.wait_until_bookmark(\"label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"As the model's confidence increases, we can watch the prediction dot move up the sigmoid curve towards a value closer to one.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3860", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene312(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Define the visual elements\n        title = Title(\"Animate the process of tracing tensor sh\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        # Narrated animation sequence\n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid activation function on our coordinate axes, \"\n                 \"<bookmark mark='show_label'/> defined mathematically as sigma of x equals one divided by, \"\n                 \"one plus e to the power of negative x. <bookmark mark='move_dot'/> Let's observe how a point \"\n                 \"moves along this curve as the input value increases.\"\n        ) as tracker:\n            # Initial setup animations\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            \n            # Reveal the mathematical label\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            \n            # Animate the dot moving along the curve\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3820", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene66(VoiceoverScene):\n    def construct(self):\n        # Initialize voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the maximum likelihood estimation (MLE) process for bigram probabilities, showing counts being divided by row sums.\n        title = Title(\"Animate the maximum likelihood estimatio\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid function, \"\n                 \"<bookmark mark='show_formula'/> along with its mathematical formula, sigma of x equals \"\n                 \"one divided by the quantity one plus e to the negative x. <bookmark mark='show_dot'/> \"\n                 \"We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_formula\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3877", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene142(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show a histogram of gradients overlaying a histogram of weights, comparing update magnitude to current magnitude.\n        title = Title(\"Show a histogram of gradients overlaying\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes to visualize the sigmoid function. \"\n                 \"<bookmark mark='show_elements'/> Here is the curve, defined by the formula \"\n                 \"sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Notice how a point moves smoothly along the curve \"\n                 \"as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_3880", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene49(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the loss landscape smoothing effect of BN, transforming a rugged terrain into a bowl.\n        title = Title(\"Animate the loss landscape smoothing eff\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, sigma of x equals one over one plus e to the negative x, <bookmark mark='show_dot'/> along with a starting point on the curve. <bookmark mark='move_dot'/> Next, we watch how the point transitions smoothly along the sigmoid curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3900", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene14(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the gradient tape unwinding sequentially from the output back to the inputs.\n        title = Title(\"Show the gradient tape unwinding sequent\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function, <bookmark mark='show_elements'/> plotting the curve along with its mathematical formula, and placing a starting point at the center. <bookmark mark='move_dot'/> Watch as the point moves along the trajectory of the curve to its new position.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3929", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene17(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the softmax exponentiation step: raw logits (spiky) \u2192 exponentiated (positive) \u2192 normalized (sums to 1).\n        title = Title(\"Animate the softmax exponentiation step:\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by setting up our coordinate axes, \"\n                 \"plotting the sigmoid curve, and showing its mathematical formula: sigma of x equals one divided by the quantity one plus e to the power of negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"<bookmark mark='move_dot'/> Now, watch how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3939", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene361(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the \"learnable scale and shift\" parameters (gamma, beta) re-introducing flexibility after normalization.\n        title = Title(\"Illustrate the \\\"learnable scale and shif\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by illustrating how learnable scale and shift parameters reintroduce flexibility after normalization. \"\n                 \"<bookmark mark='show_axes'/> Here, we set up our coordinate axes, plot the sigmoid activation function, sigma of x, \"\n                 \"and place a reference point on the curve.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_axes\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, we can observe how the point shifts along the trajectory of the curve, representing a change in the activation state.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3999", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene99(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate an element-wise multiplication between a (3,1) and (1,3) tensor, showing the resulting (3,3) outer product.\n        title = Title(\"Animate an element-wise multiplication b\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by, one plus e to the power of negative x. \"\n                 \"<bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_3970", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene165(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize a causal mask in a matrix (upper triangle filled with -inf, lower triangle with 0) used to prevent future token cheating.\n        title = Title(\"Visualize a causal mask in a matrix (upp\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We set up our coordinate axes and plot the sigmoid activation function, <bookmark mark='label'/> which is defined mathematically on the top right. <bookmark mark='dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe the dot as it moves along the sigmoid curve.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4019", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene164(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the warmup phase where running stats are unreliable (early epochs) vs. stable later.\n        title = Title(\"Illustrate the warmup phase where runnin\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We set up our coordinate system, plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Watch as the state, represented by the red dot, transitions along the curve to its stable position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4029", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene7(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the norm of gradients as a vertical bar per layer, ideally all bars of similar height (balanced).\n        title = Title(\"Visualize the norm of gradients as a ver\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid activation function, defined as one divided by, one plus e to the power of negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4042", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene365(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Create a plot of the first principal component of the weight matrix over time, showing its direction drifting.\n        title = Title(\"Create a plot of the first principal com\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function. <bookmark mark='show_label'/> Here is its mathematical definition, sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the trajectory of the curve as the input increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4043", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene204(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the gradient flow through a softmax layer, highlighting the competition between classes.\n        title = Title(\"Show the gradient flow through a softmax\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by visualizing the activation function, showing the gradient flow through a softmax layer. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as sigma of x equals one divided by, \"\n                 \"one plus e to the power of negative x, and place a tracker dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Watch as the dot moves along the curve, representing the change in activation value.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4054", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene171(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize a 3D hyperparameter landscape (hidden_dim vs. LR), with the optimal point marked by a star.\n        title = Title(\"Visualize a 3D hyperparameter landscape \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by setting up our coordinate axes, \"\n                 \"<bookmark mark='show_curve'/> then we plot the sigmoid curve, defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center, where x is zero and y is zero point five.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"<bookmark mark='move_dot'/> Now, watch as the dot moves along the curve towards the right.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=tracker.duration\n            )\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4045", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene175(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the bias gradient `dL/db` as the sum of gradients over the batch dimension.\n        title = Title(\"Show the bias gradient `dL/db` as the su\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function, which maps real-valued inputs to a range between zero and one. <bookmark mark='show_elements'/> Here, we show the coordinate axes, the sigmoid curve, its mathematical formula, and a point starting at the origin. <bookmark mark='move_dot'/> Now, watch as the point moves along the curve to represent a change in the input value.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4056", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene363(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the batch statistics vs. running statistics side-by-side during training (batch is noisy, running is smooth).\n        title = Title(\"Show the batch statistics vs. running st\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_label'/> The function is defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the sigmoid trajectory as the input value increases.\"\n        ) as tracker:\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))),\n                run_time=tracker.duration\n            )\n        \n        self.wait(1)", "status": "success"}
{"id": "sample_4069", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene155(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate a line graph showing the receptive field size as a function of number of layers and dilation rate.\n        title = Title(\"Animate a line graph showing the recepti\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_label'/> This function is defined as sigma of x equals one divided by the sum of one and e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center where x is zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the value changes as we move the dot along the curve towards the right, where x equals two.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4148", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene392(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the `nn.Module` tree: a root container expanding into submodules recursively.\n        title = Title(\"Animate the `nn.Module` tree: a root con\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system. <bookmark mark='show_elements'/> Here is the sigmoid function, sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='move_dot'/> Watch how the point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(\n                Write(title), \n                Create(ax), \n                Create(curve), \n                Write(label), \n                FadeIn(dot)\n            )\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=2\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4100", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene101(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the effect of bias in a linear layer: shifting the decision boundary in a 2D classification plot.\n        title = Title(\"Show the effect of bias in a linear laye\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_elements'/> to plot the sigmoid activation function, which maps any real-valued number to a value between zero and one. We also place a red dot at the origin. <bookmark mark='move_dot'/> Now, let's observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4133", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene43(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the effect of label smoothing on the loss distribution, flattening the peaks.\n        title = Title(\"Visualize the effect of label smoothing \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function, which is often used to map real-valued numbers into probabilities. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Let us observe how the output changes as we move a point along this curve towards the positive direction.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4156", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene314(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the running statistics being saved in the model state dictionary, highlighted in blue.\n        title = Title(\"Show the running statistics being saved \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the function. \"\n                 \"<bookmark mark='show_sigmoid'/> Here we plot the sigmoid function, defined as sigma of x equals \"\n                 \"one divided by the quantity one plus e to the power of negative x. \"\n                 \"<bookmark mark='move_dot'/> Watch as the red dot moves along the curve, representing the change in value.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4194", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene355(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize a learning rate scheduler (Cosine Annealing) as a sinusoidal curve with the LR value updating on top.\n        title = Title(\"Visualize a learning rate scheduler (Cos\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid activation function, defined as sigma of x equals one divided by one plus e to the power of negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4202", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene271(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show saturation points on the Tanh curve where local gradient is near zero (flat regions).\n        title = Title(\"Show saturation points on the Tanh curve\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the saturation points on the curve where the local gradient is near zero. Here is the plot of the sigmoid function, along with its mathematical formula and a starting point. <bookmark mark='move_dot'/> Watch as the point moves along the curve towards the saturated region where the gradient becomes flat.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4199", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene284(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the running mean and variance as two lines converging to the population statistics over time.\n        title = Title(\"Visualize the running mean and variance \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes <bookmark mark='show_sigmoid'/> and plotting the sigmoid function, defined as sigma of x equals one divided by the quantity, one plus e to the negative x. <bookmark mark='move_dot'/> Let us also place a red dot at the center and watch it trace along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4211", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene179(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize gradient clipping in action: a huge gradient vector being truncated to a maximum norm (dashed circle).\n        title = Title(\"Visualize gradient clipping in action: a\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the sigmoid function. <bookmark mark='show_all'/> We plot the curve, defined as sigma of x equals one divided by the quantity one plus e to the negative x, and place a point at the center. <bookmark mark='move_dot'/> Now, we observe the point as it moves along the curve towards the right.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_all\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4232", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene20(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create a 3D animation of adding a bias vector (1, 20) to a batch of features (128, 20), expanding across the batch dimension.\n        title = Title(\"Create a 3D animation of adding a bias v\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes. <bookmark mark='show_curve'/> Next, we plot the sigmoid curve, display its formula, sigma of x equals one divided by one plus e to the negative x, and place a red dot at the center. <bookmark mark='move_dot'/> Watch as the dot moves along the curve to a new position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4233", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene138(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the evaluation mode: the batch mean/variance are ignored, and the fixed running mean/variance are used instead.\n        title = Title(\"Animate the evaluation mode: the batch m\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"In evaluation mode, the batch mean and variance are ignored, and the fixed running mean and variance are used instead. \"\n                 \"<bookmark mark='show_sigmoid'/> Here, we visualize the sigmoid function, sigma of x, which maps inputs to a range between zero and one. \"\n                 \"<bookmark mark='move_dot'/> Let's trace the curve by moving a point along it.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4242", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene157(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the gradient signal-to-noise ratio improving with BN, shown as a cleaner gradient vector.\n        title = Title(\"Visualize the gradient signal-to-noise r\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the gradient signal-to-noise ratio. Let's set up our coordinate axes, \"\n                 \"<bookmark mark='curve'/> plot the sigmoid activation function, \"\n                 \"<bookmark mark='label'/> and place a starting point on the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"curve\")\n            self.play(Create(curve))\n            self.wait_until_bookmark(\"label\")\n            self.play(Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the point moves along the curve <bookmark mark='move'/> as we transition to a higher input value.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4257", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene194(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the `torchinfo` summary table being generated, expanding rows for each layer with parameter counts.\n        title = Title(\"Animate the `torchinfo` summary table be\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. First, we set up our coordinate axes <bookmark mark='show_sigmoid'/> and plot the sigmoid curve, defined as sigma of x equals one divided by the sum of one and e to the negative x. We also place a red dot at the center. <bookmark mark='move_dot'/> Now, watch as the dot moves along the curve towards the right, demonstrating how the function approaches one as x increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4261", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene253(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the effect of BN on a single neuron's pre-activation distribution across a batch: from shifted/scaled to centered/unit-variance.\n        title = Title(\"Show the effect of BN on a single neuron\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let's visualize the sigmoid activation function. We start by setting up our coordinate axes \"\n                 \"<bookmark mark='show_curve'/> and plotting the sigmoid curve, defined as sigma of x equals \"\n                 \"one divided by the sum of one and e to the negative x. <bookmark mark='show_dot'/> We also \"\n                 \"place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the output changes as we move the input along the curve <bookmark mark='move_dot'/> towards the right.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4289", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene41(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the effect of beta1 and beta2 on the update trajectory using a 2D contour plot.\n        title = Title(\"Illustrate the effect of beta1 and beta2\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"To illustrate the effect of beta one and beta two, we first set up our coordinate axes, plot the sigmoid activation function, and place a red dot at the center. <bookmark mark='MOVE_DOT'/> Then, we watch the dot transition along the curve as its position updates.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"MOVE_DOT\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4297", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene10(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the gradient w.r.t gamma (scale) and beta (shift) as simple summations over the batch dimension.\n        title = Title(\"Illustrate the gradient w.r.t gamma (sca\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes, <bookmark mark='plot_curve'/> then we plot the sigmoid curve, which is defined as sigma of x equals one divided by one plus e to the negative x, and place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"plot_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4316", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene331(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the sampling process: a spinning wheel divided into character slices, landing on the next character based on learned probabilities.\n        title = Title(\"Animate the sampling process: a spinning\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> which is defined as sigma of x equals one over one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center where x is zero and the output is zero point five.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Next, we animate the dot moving along the curve <bookmark mark='move_dot'/> to show how the output approaches one as x increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4327", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene192(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize gradient flow through layer normalization, dividing the stream by the input's standard deviation.\n        title = Title(\"Visualize gradient flow through layer no\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_elements'/> to visualize the sigmoid function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x. We also place a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4333", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene73(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize matrix multiplication (`matmul`) as a 3D animation where rows of A slide over columns of B, with dot products lighting up the output matrix.\n        title = Title(\"Visualize matrix multiplication (`matmul\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"In this visualization, we explore the sigmoid activation function, <bookmark mark='show_elements'/> which maps any real-valued number to a value between zero and one. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4337", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene359(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the accumulation of gradients at a branching node where two paths meet.\n        title = Title(\"Illustrate the accumulation of gradients\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by illustrating the accumulation of gradients. Let's set up our coordinate system, <bookmark mark='show_curve'/> plot the sigmoid activation function, <bookmark mark='show_dot'/> and place a starting point on the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let's trace the path <bookmark mark='move_dot'/> as the point moves along the sigmoid curve.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4382", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene133(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the receptive field's \"left context\" growing as the model gets deeper.\n        title = Title(\"Visualize the receptive field's \\\"left co\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let's visualize the sigmoid function. We set up our axes, plot the curve, and write its mathematical definition, sigma of x equals one over one plus e to the negative x, <bookmark mark='show_dot'/> placing a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, we can observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4384", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene81(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the ratio of parameter updates to parameter values (`update/param`) per layer, ideally ~1e-3.\n        title = Title(\"Show the ratio of parameter updates to p\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system. <bookmark mark='show_elements'/> Here, we plot the sigmoid function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. We also place a red dot at the center. <bookmark mark='move_dot'/> Now, let us observe how this dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4385", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene158(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a comparison of activation histograms after forward pass: Xavier init (stable) vs. standard init (shrinking/exploding).\n        title = Title(\"Show a comparison of activation histogra\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid activation function on our coordinate axes. <bookmark mark='move_dot'/> Watch how the activation value increases as we move the input point to the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4395", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene335(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create an animated bar chart comparing the NLL loss contributions of each character in a sequence.\n        title = Title(\"Create an animated bar chart comparing t\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by displaying the title, setting up our coordinate axes, and plotting the sigmoid function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x. We also place a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Next, we animate the dot as it moves along the curve towards the right, approaching its asymptotic value of one.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4355", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene346(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the gated activation: Tanh (magnitude) \u00d7 Sigmoid (gate) element-wise.\n        title = Title(\"Illustrate the gated activation: Tanh (m\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by illustrating the gated activation mechanism. Let's set up our coordinate axes. \"\n                 \"<bookmark mark='plot_curve'/> Next, we plot the sigmoid function, which acts as our gating mechanism. \"\n                 \"<bookmark mark='show_label'/> The sigmoid function, sigma of x, is defined as one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Let's observe how the activation value changes as we move along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"plot_curve\")\n            self.play(Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4401", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene213(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the reduction in sensitivity to initialization, showing multiple initializations converging to the same valley with BN.\n        title = Title(\"Visualize the reduction in sensitivity t\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> and plotting the sigmoid activation function, defined as sigma of x equals one divided by the sum of one and e raised to the power of negative x. <bookmark mark='show_dot'/> We also place an initial point on the curve. <bookmark mark='move_dot'/> Watch as this point smoothly transitions along the trajectory of the function.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4421", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene169(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        title = Title(\"Animate the derivation of the derivative\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid function, \"\n                 \"defined as sigma of x equals one divided by, one plus e to the power of negative x. \"\n                 \"We also place a red dot at the center, where x equals zero. \"\n                 \"<bookmark mark='dot_move'/> Watch as the dot moves along the curve to the right, \"\n                 \"approaching its upper limit.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4446", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene173(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the numerical stability of softmax by subtracting the maximum logit, shown as shifting the entire curve down.\n        title = Title(\"Illustrate the numerical stability of so\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, plotting the sigmoid function, and placing a point at the center. <bookmark mark='move_dot'/> Next, we watch the point transition along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4479", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene326(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the gradient distribution for Layer 5 vs. Layer 1 as two side-by-side histograms, highlighting vanishing.\n        title = Title(\"Visualize the gradient distribution for \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the behavior of a function. We begin by setting up our coordinate axes <bookmark mark='show_curve'/> and plotting the sigmoid activation curve, defined as sigma of x equals one divided by the quantity one plus e to the negative x. We also place a red dot at the center. <bookmark mark='move_dot'/> Now, watch as the dot moves along the curve, demonstrating how the output changes as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4476", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene51(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize a 3D stack of activation histograms for all layers, rotating to show the distribution changes.\n        title = Title(\"Visualize a 3D stack of activation histo\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, <bookmark mark='show_label'/> defined as sigma of x equals one over one plus e to the negative x. We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4480", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene31(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate a 2D contour plot where the optimizer takes steps perpendicular to the contour lines (steepest descent).\n        title = Title(\"Animate a 2D contour plot where the opti\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='show_elements'/> We display the axes, the curve, and place a red dot at the center. <bookmark mark='move_dot'/> Now, we animate the dot as it moves along the sigmoid curve.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(\n                Write(title), \n                Create(ax), \n                Create(curve), \n                Write(label), \n                FadeIn(dot)\n            )\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=2\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4517", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene372(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create a step-by-step animation of a scalar-valued computational graph where each node displays both its value and its local derivative.\n        title = Title(\"Create a step-by-step animation of a sca\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by setting up our coordinate axes, \"\n                 \"<bookmark mark='plot_curve'/> plotting the characteristic S-shaped curve, and displaying its mathematical formula. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the point where the input is zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"plot_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the output value changes as we slide the input value to the right, \"\n                 \"<bookmark mark='move_dot'/> moving the dot along the curve towards an input of two.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4547", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene378(VoiceoverScene):\n    def construct(self):\n        # Initialize voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the gradient of Tanh: `d/dx tanh(x) = 1 - tanh(x)^2`, showing the inverted parabola shape.\n        title = Title(\"Visualize the gradient of Tanh: `d/dx ta\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system. <bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as sigma of x equals one over one plus e to the negative x, and place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, watch as the dot moves along the curve, showing how the function value increases as x grows.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4544", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene228(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the effect of Xavier on gradients: backward pass variance also maintained, shown as equal bar heights.\n        title = Title(\"Visualize the effect of Xavier on gradie\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by setting up our coordinate axes and placing a point at the center. \"\n                 \"<bookmark mark='show_curve'/> Next, we plot the sigmoid curve, defined as sigma of x equals one divided by the sum of one and e raised to the power of negative x. \"\n                 \"<bookmark mark='move_dot'/> Finally, we can observe how the point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4525", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene185(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Original objects\n        title = Title(\"Animate the update equation: `running_me\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        # Voiceover and animations\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid curve. \"\n                 \"<bookmark mark='show_math'/> The mathematical formula is sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Now, let us watch a point trace along the curve as it transitions to a higher value.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            \n            self.wait_until_bookmark(\"show_math\")\n            self.play(Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4577", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene303(VoiceoverScene):\n    def construct(self):\n        # Setup voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Display a 26x26 character transition matrix as a heatmap, where each cell's color intensity represents the log-frequency of a character pair.\n        title = Title(\"Display a 26x26 character transition mat\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. <bookmark mark='axes'/> We set up our coordinate axes and plot the sigmoid curve, <bookmark mark='label'/> defined as sigma of x equals one divided by, one plus e to the power of negative x. <bookmark mark='dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"axes\")\n            self.play(Create(ax), Create(curve))\n            self.wait_until_bookmark(\"label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve as x increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4636", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene152(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the performance speedup of tensor-level gradients (GPU) vs. scalar loops (CPU) using a racing bar chart.\n        title = Title(\"Visualize the performance speedup of ten\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our visualization. <bookmark mark='show_all'/> Here we display the title, the coordinate axes, the sigmoid curve, its mathematical formula, and a red dot at the center. <bookmark mark='move_dot'/> Now, let's observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_all\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4650", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene140(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Create a real-time line plot of training loss and a separate bar chart of gradient norm during a single epoch.\n        title = Title(\"Create a real-time line plot of training\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by setting up our coordinate axes, \"\n                 \"plotting the characteristic S-shaped curve, and displaying its mathematical formula: \"\n                 \"sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Now, let's trace the curve by moving a point along it.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4640", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene307(VoiceoverScene):\n    def construct(self):\n        # Set up the speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the nearest-neighbor search: taking a character embedding and finding its closest neighbors in the embedding space.\n        title = Title(\"Visualize the nearest-neighbor search: t\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid function. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as sigma of x \"\n                 \"equals one divided by the quantity one plus e to the negative x, and place a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, watch as the dot moves along the curve towards its upper limit.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4659", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene288(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the `to(device)` method moving all parameters and buffers from CPU to GPU.\n        title = Title(\"Animate the `to(device)` method moving a\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"which is defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Now, let's trace the curve by moving a point along it.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4644", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene265(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the hook system: a forward hook triggering to log activations, and a backward hook logging gradients.\n        title = Title(\"Animate the hook system: a forward hook \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We can visualize the forward pass of a neural network layer using a sigmoid activation function. Let's set up our coordinate system <bookmark mark='show_elements'/> and plot the sigmoid curve, along with its mathematical formula. We also place a dot representing our activation value. <bookmark mark='move_dot'/> Now, as the input changes, we can track the activation value moving along the curve.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4669", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene28(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize regularization (L2) shrinking weights, displayed as a compression of the weight distribution histogram toward zero.\n        title = Title(\"Visualize regularization (L2) shrinking \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid activation function. \"\n                 \"<bookmark mark='show_axes'/> Here, we set up our coordinate axes, \"\n                 \"<bookmark mark='show_curve'/> plot the characteristic S-shaped curve, \"\n                 \"<bookmark mark='show_label'/> display the formula for sigma of x, \"\n                 \"and place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_axes\")\n            self.play(Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label), FadeIn(dot))\n\n        with self.voiceover(\n            text=\"<bookmark mark='move_dot'/> Next, we watch the dot transition along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=tracker.duration\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4672", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene222(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate overfitting as a high-variance scenario: a complex polynomial oscillating wildly to pass through every noise point.\n        title = Title(\"Animate overfitting as a high-variance s\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. We start by setting up our coordinate axes, \"\n                 \"<bookmark mark='show_curve'/> plotting the sigmoid curve, defined as sigma of x equals one over \"\n                 \"one plus e to the negative x, <bookmark mark='show_dot'/> and placing a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, watch as the dot moves along the curve to its new position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4689", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene125(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the forward pass sequentially lighting up each layer's activation statistics panel.\n        title = Title(\"Animate the forward pass sequentially li\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system. <bookmark mark='show_sigmoid'/> Here is the sigmoid activation function, defined as sigma of x equals one over, one plus e to the negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4716", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene60(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Create a parallel coordinates plot tracking multiple runs with different hyperparameters.\n        title = Title(\"Create a parallel coordinates plot track\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. We start by setting up our coordinate axes, plotting the sigmoid curve, and placing a red dot at the y-intercept. <bookmark mark='move_dot'/> Next, we watch the dot move along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4719", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene287(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the process of reducing the learning rate when loss spikes, visualized as turning a dial.\n        title = Title(\"Illustrate the process of reducing the l\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes. <bookmark mark='show_curve'/> \"\n                 \"Next, we plot the sigmoid function, defined as sigma of x equals one divided by \"\n                 \"one plus e to the negative x. <bookmark mark='show_dot'/> We place a red dot at the center of the curve. \"\n                 \"<bookmark mark='move_dot'/> Finally, we animate the dot as it moves along the curve to the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4738", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene262(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the pre-activation (before activation function) histogram turning from a bell curve to a skewed distribution.\n        title = Title(\"Animate the pre-activation (before activ\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"In this visualization, we explore the sigmoid activation function. \"\n                 \"<bookmark mark='show_elements'/> We begin by setting up our coordinate axes, \"\n                 \"plotting the sigmoid curve, and placing a point at the origin. \"\n                 \"<bookmark mark='move_dot'/> Watch as the point moves along the curve, \"\n                 \"demonstrating how the function maps input values to outputs between zero and one.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4724", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene200(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize a WaveNet block: input \u2192 Dilated Conv \u2192 Tanh/Sigmoid (gated) \u2192 1x1 Conv \u2192 + Input (residual).\n        title = Title(\"Visualize a WaveNet block: input \\u2192 Dilat\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_curve'/> and plotting the sigmoid activation function, defined as sigma of x equals one divided by, one plus e to the power of negative x. <bookmark mark='move_dot'/> Let's observe how the output changes as we move a point along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4759", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene237(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the input sequence (characters) being passed through an embedding layer, transforming discrete IDs into dense vectors.\n        title = Title(\"Illustrate the input sequence (character\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"First, we display the title and set up our coordinate axes. \"\n            \"<bookmark mark='draw_curve'/> Next, we plot the sigmoid curve, defined as sigma of x \"\n            \"equals one divided by the quantity one plus e to the negative x, and place a red dot at the center. \"\n            \"<bookmark mark='move_dot'/> Finally, we watch the dot move along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            \n            self.wait_until_bookmark(\"draw_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4784", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene320(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate a 2D grid of features, applying scale (zoom) and shift (translate) as affine transformations.\n        title = Title(\"Animate a 2D grid of features, applying \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid curve. \"\n                 \"<bookmark mark='show_label'/> Next, we display the mathematical formula for the \"\n                 \"sigmoid function and place a red dot at the starting position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label), FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, we animate the dot as it moves along the curve <bookmark mark='move_dot'/> to its new position.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4787", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene214(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the downsampling/upsampling paths in a hierarchical decoder structure.\n        title = Title(\"Visualize the downsampling/upsampling pa\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes, plotting the sigmoid curve, and displaying its mathematical definition: sigma of x equals one divided by, one plus e to the negative x. We also place a red dot at the center. <bookmark mark='move_dot'/> Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4792", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene131(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize gradient flow through a deep 20-layer network with and without BN, comparing gradient norm bars.\n        title = Title(\"Visualize gradient flow through a deep 2\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by drawing the coordinate axes, plotting the sigmoid curve, and showing its mathematical formula, sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='move_dot'/> Next, we can observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4826", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene282(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the loss surface curvature using a 2D slice, with the optimizer position and local curvature estimated by the Hessian.\n        title = Title(\"Visualize the loss surface curvature usi\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the loss surface curvature. We plot the sigmoid function on our coordinate axes, <bookmark mark='show_dot'/> along with a red dot representing the optimizer's initial position.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"As the optimization process proceeds, the optimizer moves along the curve <bookmark mark='move_dot'/> to its new position.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4879", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene319(VoiceoverScene):\n    def construct(self):\n        # Initialize voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the classic train/dev loss curves: underfitting (both high), overfitting (train low, dev high), ideal (both low and close).\n        title = Title(\"Show the classic train/dev loss curves: \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by setting up our coordinate axes, \"\n            \"<bookmark mark='show_curve'/> plotting the sigmoid curve, and writing its mathematical definition: \"\n            \"sigma of x equals one divided by, one plus e to the negative x. We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, watch as the dot moves along the curve towards the right, demonstrating how the function approaches one as x increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_4847", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene234(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the embedding backward pass: only rows corresponding to the current batch indices receive gradients.\n        title = Title(\"Visualize the embedding backward pass: o\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by displaying the title. <bookmark mark='show_axes'/> Next, we set up our coordinate axes, plot the sigmoid curve, and display its mathematical formula, where sigma of x equals one divided by the sum of one and e to the power of negative x. <bookmark mark='show_dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_axes\")\n            self.play(Create(ax), Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, we watch the dot transition along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4884", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene198(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the residual pathway as a thick highway preserving the gradient flow.\n        title = Title(\"Show the residual pathway as a thick hig\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, represented as sigma of x equals one divided by one plus e to the negative x. <bookmark mark='move_dot'/> Let us trace the activation value as we move the point along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4897", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene16(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the freezing of BN parameters after training, locking gamma and beta values.\n        title = Title(\"Illustrate the freezing of BN parameters\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"To illustrate the freezing of batch normalization parameters after training, we lock the gamma and beta values, establishing a fixed transformation. <bookmark mark='show_plot'/> Here, we visualize this fixed state using a sigmoid activation curve, where the mapping is now constant. <bookmark mark='move_dot'/> Watch as the input point moves along this locked trajectory, representing deterministic inference.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_plot\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4978", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene260(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the forward pass of a BN layer with a mini-batch of 4 samples, showing individual sample paths.\n        title = Title(\"Animate the forward pass of a BN layer w\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one over, one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> We also place a red dot at the center, \"\n                 \"which we then animate along the curve to show how the output changes as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4942", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene108(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate a typewriter effect displaying a 50-character string sampled from the bigram model.\n        title = Title(\"Animate a typewriter effect displaying a\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system, <bookmark mark='show_sigmoid'/> and plotting the sigmoid function, defined as sigma of x equals one over one plus e to the negative x. <bookmark mark='move_dot'/> Let's observe how a point on this curve moves as x increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4955", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene353(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Illustrate the topological ordering of nodes in a computational graph, numbering each operation sequentially from inputs to output.\n        title = Title(\"Illustrate the topological ordering of n\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us illustrate the sigmoid function, which is commonly used in computational graphs. \"\n                 \"We begin by setting up our coordinate axes, plotting the sigmoid curve, and showing its mathematical formula, \"\n                 \"sigma of x equals one divided by the quantity one plus e to the negative x. We also place a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Next, we can observe how the value changes as we move the dot along the curve towards the right.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5010", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene230(VoiceoverScene):\n    def construct(self):\n        # Setup voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Show the batch dimension flowing through the model, with a stack of parallel arrows representing each sample.\n        title = Title(\"Show the batch dimension flowing through\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, \"\n                 \"defined as sigma of x equals one divided by one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Let's track a point as it moves along this activation curve.\"\n        ) as tracker:\n            self.play(\n                Write(title), \n                Create(ax), \n                Create(curve), \n                Write(label), \n                FadeIn(dot)\n            )\n            \n            self.wait_until_bookmark(\"move_dot\")\n            \n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=2\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_4989", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene132(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the difference between `view` (shared data) and `clone` (copy) using two separate color-coded memory blocks.\n        title = Title(\"Animate the difference between `view` (s\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"In this demonstration, we explore the sigmoid activation function. \"\n                 \"<bookmark mark='axes_and_curve'/> We start by setting up our coordinate axes \"\n                 \"and plotting the characteristic S-shaped curve, defined by the formula \"\n                 \"sigma of x equals one divided by the quantity, one plus e to the negative x. \"\n                 \"We also place a red dot at the center. \"\n                 \"<bookmark mark='dot_move'/> Watch as we track this point along the curve, \"\n                 \"showing how it smoothly transitions towards one as x increases.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"axes_and_curve\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_5016", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene238(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the weight update direction calculated from the gradient of the linear layer.\n        title = Title(\"Illustrate the weight update direction c\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"To illustrate the weight update direction, we first set up our coordinate system and plot the sigmoid activation function. <bookmark mark='show_label'/> This function is defined as sigma of x equals one over one plus e to the negative x. <bookmark mark='show_dot'/> We also place a starting point on the curve at x equals zero.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, we observe how the point moves along the curve as the weight is updated.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5037", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene264(VoiceoverScene):\n    def construct(self):\n        # Setup speech service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the computation of the cross-entropy loss as: Logits \u2192 Softmax \u2192 Log \u2192 Negative Mean, showing intermediate tensors.\n        title = Title(\"Visualize the computation of the cross-e\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the sigmoid function. Let's set up our coordinate axes, plot the sigmoid curve, and display its mathematical formula, while placing a red dot at the center. <bookmark mark='dot_move'/> Now, let's observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5036", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene366(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the effect of additive smoothing (Laplace smoothing) on the transition matrix, flattening the color distribution.\n        title = Title(\"Visualize the effect of additive smoothi\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid function, which maps any real-valued number to a value between zero and one. <bookmark mark='move_dot'/> Let us observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5027", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene232(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize the momentum component of Adam as a moving average of gradients (exponential decay of past gradients).\n        title = Title(\"Visualize the momentum component of Adam\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's visualize the momentum component of Adam. We begin by setting up our coordinate axes, \"\n                 \"<bookmark mark='show_curve'/> plotting the sigmoid activation function, sigma of x equals one divided by one plus e to the negative x, \"\n                 \"<bookmark mark='show_dot'/> and placing a starting point on the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"<bookmark mark='move_dot'/> Now, watch as the point moves along the sigmoid curve, simulating a step in our optimization process.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5023", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene162(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate the embedding table as a grid of colored tiles, where each row represents a character and each column a feature.\n        title = Title(\"Animate the embedding table as a grid of\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid function, <bookmark mark='show_label'/> defined as sigma of x equals one divided by, one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_5040", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene356(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the gradient-to-activation ratio (Grad/Activation) per layer, diagnosing training instability.\n        title = Title(\"Animate the gradient-to-activation ratio\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to analyze the gradient-to-activation ratio. \"\n                 \"<bookmark mark='show_sigmoid'/> Here, we plot the sigmoid activation function, defined as sigma of x equals one over one plus e to the negative x. \"\n                 \"<bookmark mark='show_dot'/> We place a red dot at the initial activation value of zero point five.\"\n         ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_sigmoid\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let's observe how the activation value changes as we move along the curve.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5058", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene122(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the creation of a custom `Embedding` layer with a weight matrix and a forward lookup.\n        title = Title(\"Visualize the creation of a custom `Embe\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes, plotting the sigmoid curve, and writing its mathematical formula. <bookmark mark='move_dot'/> Now, let us observe how a point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5084", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene74(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the gradient noise scale (`grad_noise = grad_std / grad_norm`) as a scatter plot over steps.\n        title = Title(\"Illustrate the gradient noise scale (`gr\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function. \"\n                 \"<bookmark mark='show_label'/> The function is defined as sigma of x equals one divided by the sum of one and e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Now, let us observe how a point moves along this curve as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5144", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene178(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Animate vanishing gradients in a 10-layer network: the first layer's gradient updates are microscopic (tiny arrows), the last layer's are large (big arrows).\n        title = Title(\"Animate vanishing gradients in a 10-laye\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"To understand the vanishing gradient problem, let us first visualize the sigmoid activation function, \"\n                 \"denoted as sigma of x, which maps inputs to values between zero and one. \"\n                 \"<bookmark mark='move_dot'/> Watch how the output changes as we move along the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5047", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene11(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the conversion of a text string into a sequence of one-hot vectors (spike trains).\n        title = Title(\"Animate the conversion of a text string \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid function, <bookmark mark='show_elements'/> which maps any real-valued number to a value between zero and one. Here, we have the axes, the blue curve, its mathematical formula, and a red dot at the center. <bookmark mark='move_dot'/> Let's observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5146", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene389(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the forward pass of a custom `Block` module consisting of Linear, BN, and ReLU.\n        title = Title(\"Animate the forward pass of a custom `Bl\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid activation function, \"\n                 \"defined as sigma of x equals one over, one plus e to the negative x, and place a red dot at the center. \"\n                 \"<bookmark mark='move_dot'/> Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5186", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene170(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the saturation of Tanh neurons: activations near -1/1 cause gradients to vanish (represented by shrinking red arrows).\n        title = Title(\"Animate the saturation of Tanh neurons: \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us examine the saturation of activation functions. We begin by plotting the sigmoid function, defined as sigma of x equals one over one plus e to the negative x. <bookmark mark='MOVE_DOT'/> As the input increases, the activation moves along the curve, approaching its saturation limit where the gradient vanishes.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"MOVE_DOT\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5222", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene350(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Create a heatmap of gradient magnitudes across all weights in a small network after one backward pass.\n        title = Title(\"Create a heatmap of gradient magnitudes \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. <bookmark mark='show_axes'/> We plot the curve on these coordinate axes, <bookmark mark='show_formula'/> showing its mathematical formula, and <bookmark mark='show_dot'/> place a point at the origin where the output is exactly zero point five.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_axes\")\n            self.play(Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_formula\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"<bookmark mark='move_dot'/> Now, as we increase the input, notice how the point smoothly transitions towards its upper limit of one.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_5174", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene332(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a word cloud of the most frequently predicted character transitions in the validation set.\n        title = Title(\"Show a word cloud of the most frequently\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes, <bookmark mark='show_curve'/> plotting the sigmoid curve, <bookmark mark='show_label'/> and writing its mathematical definition: sigma of x equals one over, one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve <bookmark mark='move_dot'/> as the input value x increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5239", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene276(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the root-mean-square (RMS) of gradients per layer as a line chart, ideally staying constant.\n        title = Title(\"Visualize the root-mean-square (RMS) of \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system to visualize the sigmoid function. \"\n                 \"<bookmark mark='show_elements'/> Here, we plot the sigmoid curve, defined as sigma of x \"\n                 \"equals one divided by the quantity one plus e to the negative x, and place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Watch as the dot moves along the curve, demonstrating how the output approaches one as the input increases.\"\n        ) as tracker:\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=tracker.duration\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5242", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene248(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the inference forward pass using the frozen running stats, with a lock icon over them.\n        title = Title(\"Visualize the inference forward pass usi\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by visualizing the inference forward pass using the frozen running statistics. \"\n                 \"<bookmark mark='show_elements'/> Here, we set up our coordinate axes, plot the sigmoid activation function, \"\n                 \"and place a starting point at the center.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            \n        with self.voiceover(\n            text=\"Now, let us observe how the input value propagates through the function, \"\n                 \"<bookmark mark='move_dot'/> moving the point along the sigmoid curve to its final activated state.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5245", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene123(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the index selection process: a vector of indices selects specific rows from the gradient table.\n        title = Title(\"Animate the index selection process: a v\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid activation function. We begin by setting up our coordinate axes. <bookmark mark='show_curve'/> Next, we plot the sigmoid curve, which is defined as one divided by the sum of one and e to the negative x. <bookmark mark='move_dot'/> Finally, we can observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5243", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene235(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize underfitting as a high-bias scenario: a straight line unable to fit a sinusoidal dataset.\n        title = Title(\"Visualize underfitting as a high-bias sc\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize a function on these coordinate axes. \"\n                 \"<bookmark mark='show_curve'/> Here, we plot the sigmoid function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the power of negative x. \"\n                 \"<bookmark mark='show_dot'/> We also place a red dot at the center.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"<bookmark mark='move_dot'/> Watch as the dot moves smoothly along the curve to its new position.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5258", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene251(VoiceoverScene):\n    def construct(self):\n        # Set up the voiceover service\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate a scrolling dashboard showing mean, std, min, and max of activations for each layer during forward pass.\n        title = Title(\"Animate a scrolling dashboard showing me\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> defined as sigma of x equals one divided by, one plus e to the power of negative x. \"\n                 \"<bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5274", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene141(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a cyclic learning rate schedule as a sawtooth wave oscillating between min and max bounds.\n        title = Title(\"Show a cyclic learning rate schedule as \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by the quantity one plus e to the negative x. \"\n                 \"We also place a red dot at the center. <bookmark mark='move_dot'/> \"\n                 \"Now, we transition the dot along the curve to demonstrate its behavior as x increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5296", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene267(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize a learning rate range test: loss plotted against LR on a log-scale, with the optimal range highlighted.\n        title = Title(\"Visualize a learning rate range test: lo\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes, plotting the sigmoid function, and displaying its formula, sigma of x. <bookmark mark='move_dot'/> Then, we watch the red dot transition along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5321", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene202(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the batch-level gradient averaging over the mini-batch dimension.\n        title = Title(\"Visualize the batch-level gradient avera\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"Let's visualize the batch-level gradient averaging over the mini-batch dimension. \"\n                 \"First, we set up our coordinate system <bookmark mark='show_curve'/> and plot the sigmoid activation function, \"\n                 \"defined as sigma of x equals one divided by, one plus e to the negative x. \"\n                 \"<bookmark mark='move_dot'/> Now, watch how the data point moves along the curve as the input increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n\n        self.wait(1)", "status": "success"}
{"id": "sample_5292", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene394(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show the accumulation of gradient contributions if the same index appears multiple times in a batch.\n        title = Title(\"Show the accumulation of gradient contri\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_graph'/> and plotting the sigmoid function, defined as sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='move_dot'/> Now, let us observe how a point moves along this curve as its input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_graph\")\n            self.play(Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5313", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene243(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Visualize Adam's update rule compared to vanilla SGD on a pathological saddle point (SGD oscillates, Adam escapes).\n        title = Title(\"Visualize Adam's update rule compared to\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let us visualize the sigmoid function. <bookmark mark='show_all'/> We plot the curve, defined as sigma of x equals one divided by the quantity one plus e to the negative x, and place a red dot at the center. <bookmark mark='move_dot'/> Watch how the dot moves along the curve as its input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_all\")\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5331", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene208(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Illustrate the memory footprint of WaveNet, growing with depth and dilation.\n        title = Title(\"Illustrate the memory footprint of WaveN\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by plotting the sigmoid activation function, <bookmark mark='show_elements'/> which maps any real-valued number into a range between zero and one. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(\n                Write(title), \n                Create(ax), \n                Create(curve), \n                Write(label), \n                FadeIn(dot)\n            )\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(\n                dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), \n                run_time=2\n            )\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5324", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene309(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize the batch size effect: small batches (noisy loss line) vs. large batches (smooth loss line).\n        title = Title(\"Visualize the batch size effect: small b\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system and plotting the sigmoid function, which is defined as sigma of x equals one divided by the quantity one plus e to the negative x. <bookmark mark='dot_move'/> Watch how a point moves along this activation curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"dot_move\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5345", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene67(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Animate the backward pass through Tanh: upstream gradient multiplied by the local gradient map.\n        title = Title(\"Animate the backward pass through Tanh: \")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"In this visualization, we will look at the backward pass through an activation function. \"\n                 \"Let's set up our coordinate axes, <bookmark mark='show_curve'/> plot the sigmoid activation curve, \"\n                 \"and <bookmark mark='show_dot'/> place a point on the curve. <bookmark mark='move_dot'/> \"\n                 \"Now, watch how the point moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax))\n            self.wait_until_bookmark(\"show_curve\")\n            self.play(Create(curve), Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5377", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene324(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        # Visualize a subset of test samples, displaying the model's predicted character above the actual character, with a checkmark or X.\n        title = Title(\"Visualize a subset of test samples, disp\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate axes and plotting the sigmoid activation function, \"\n                 \"<bookmark mark='show_label'/> along with its mathematical formula, sigma of x equals one divided by \"\n                 \"one plus e to the negative x. <bookmark mark='show_dot'/> We also place a red dot at the center of the curve.\"\n        ) as tracker:\n            self.play(Write(title), Create(ax), Create(curve))\n            self.wait_until_bookmark(\"show_label\")\n            self.play(Write(label))\n            self.wait_until_bookmark(\"show_dot\")\n            self.play(FadeIn(dot))\n\n        with self.voiceover(\n            text=\"Now, let us observe how the dot moves along the curve as the input value increases.\"\n        ) as tracker:\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=tracker.duration)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5346", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene150(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n        \n        # Show a \"gradient flow dashboard\" highlighting layers with near-zero gradient norms in red.\n        title = Title(\"Show a \\\"gradient flow dashboard\\\" highlig\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n        \n        with self.voiceover(\n            text=\"Let's explore the sigmoid activation function, which is widely used in neural networks. \"\n                 \"<bookmark mark='show_axes'/> Here, we set up our coordinate system and plot the characteristic S-shaped curve. \"\n                 \"<bookmark mark='show_formula'/> The mathematical definition is sigma of x equals one over one plus e to the power of negative x. We also place a marker at the origin. \"\n                 \"<bookmark mark='move_dot'/> As we increase the input, we can trace the activation level moving smoothly towards its upper bound of one.\"\n        ) as tracker:\n            self.play(Write(title))\n            \n            self.wait_until_bookmark(\"show_axes\")\n            self.play(Create(ax), Create(curve))\n            \n            self.wait_until_bookmark(\"show_formula\")\n            self.play(Write(label), FadeIn(dot))\n            \n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
{"id": "sample_5399", "narrated_manim_code": "from manim import *\nfrom manim_voiceover import VoiceoverScene\nfrom tools.aos_speech_service import AOSSpeechService\nimport numpy as np\n\nclass AOSTrajectoryScene239(VoiceoverScene):\n    def construct(self):\n        self.set_speech_service(AOSSpeechService(voice=\"alba\", cache_dir=\"voiceover_cache\"))\n\n        title = Title(\"Animate the formula for NLL loss step-by\")\n        ax = Axes(x_range=[-3, 3, 1], y_range=[-1, 5, 1], x_length=6, y_length=4)\n        curve = ax.plot(lambda x: 1 / (1 + np.exp(-x)), color=BLUE)\n        label = MathTex(r'\\sigma(x) = \\frac{1}{1 + e^{-x}}').to_corner(UR)\n        dot = Dot(ax.c2p(0, 0.5), color=RED)\n\n        with self.voiceover(\n            text=\"We begin by setting up our coordinate system <bookmark mark='show_elements'/> and plotting the sigmoid activation function, defined as sigma of x equals one divided by, one plus e to the power of negative x. <bookmark mark='move_dot'/> Let's observe how a point moves along this curve as the input value increases.\"\n        ) as tracker:\n            self.play(Write(title))\n            self.wait_until_bookmark(\"show_elements\")\n            self.play(Create(ax), Create(curve), Write(label), FadeIn(dot))\n            self.wait_until_bookmark(\"move_dot\")\n            self.play(dot.animate.move_to(ax.c2p(2, 1 / (1 + np.exp(-2)))), run_time=2)\n            \n        self.wait(1)", "status": "success"}
