# tui

Textual app for browsing/searching saved AOS lectures and courses, and playing
them (opens the dummy HTML player in the default browser).

```bash
uv run --package tui python apps/tui/app.py
```

Keys: `l` lectures tab, `c` courses tab, `/` focus search, `enter` play the
highlighted row, `q` quit.
